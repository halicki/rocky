# 2026-08-19 — Rule 4, the CTR-bound queue served for the first time (RECONSTRUCTED)

**Day**: 94 (loop-run count)
**Action**: **Rule 4 (CTR rescue)** on `/blog/when-to-book-surf-lessons-canggu/`
**Status**: ⚠️ **RECONSTRUCTED on 2026-08-20.** This run committed and deployed but never wrote a log.

---

## Why this file exists and what it does not contain

The 08-19 run shipped `f72f209` at 09:00:28 +0200 and Vercel deployed it to production
(`dpl_A2BdCAK93hUKfAzZoueRLFFYFcXs`, **READY**, aliased). It then stopped: no `docs/seo-log/2026-08-19.md`
and no INDEX line. The 08-20 run found the orphan in `git log` and reconstructed this entry.

**The GSC snapshot of that run is gone and is NOT reproduced here.** The GSC API serves a rolling
28-day window; the 08-19 window (≈2026-07-20 → 2026-08-16) cannot be re-fetched. Everything below is
taken from the commit message, the Vercel deployment record and a live smoke test run on 08-20 —
**nothing is inferred, and no totals, query table or page table is invented to fill the gap.** That
loss is permanent and is the cost of the missing log.

Precedent for reconstructing rather than skipping: the 2026-05-11 entry, whose log was likewise
never written and which was reconstructed in INDEX on 2026-05-13.

## What the run did (from the commit)

```
ACTION: Rule 4 — CTR rescue on the largest zero-click member of the
  CTR-bound queue v3 created on 08-10
TARGET: src/app/blog/when-to-book-surf-lessons-canggu/page.mdx (1 file, 4 lines)
RATIONALE (quoted from the commit): "Page sits at pos 8.9 with 37 impressions
  and 0 clicks — the biggest member of the CTR-bound queue v3 created on 08-10
  and never served in nine days."
```

Two defects named in the commit:

- description was **169 chars**, truncated by Google at ~155 (losing "Batu Bolong"), and read as a
  table of contents rather than an answer
- title payload *"(2026 Timing Guide)"* promised a guide, not an answer

| | Before | After |
|---|---|---|
| `<title>` | When to Book Surf Lessons in Canggu (2026 Timing Guide) | **When to Book Surf Lessons in Canggu: 6:30 AM, 5 Days Ahead** (58 ch) |
| `<meta description>` | 169 ch, list-of-topics form | **145 ch**, carries May/June/Sept, 6:30–9:30 AM, 5–7 days, + WhatsApp CTA |

H1, canonical, `ArticleSchema` headline, `BreadcrumbList` and `FAQPage` left unchanged so position
stays separately attributable from CTR.

Commit: `f72f209` · Deployment: `dpl_A2BdCAK93hUKfAzZoueRLFFYFcXs` → production, **READY**

**Smoke test (run 2026-08-20, not on the day): PASS** — `200`; title served as
`When to Book Surf Lessons in Canggu: 6:30 AM, 5 Days Ahead`.

## Notes carried into 08-20

- This was the **first service of the Rule 4 queue** since v3 created it on 08-10, and the first
  time in the loop's history that Rule 1 did not pre-empt it.
- **It skipped the mandatory content trigger**, which was already due 08-17. No log means no
  recorded reason. The 08-20 run treats the mandatory clause as **three days overdue**, not two.
- **Read on this action: ≥2026-08-26** (7 days). Bar: any non-zero click on a page that has never
  converted; the 07-31 rescue precedent used a 0.5% CTR bar and failed it.
