# 2026-09-22 — Read 8 closes indeterminate, Rule 1 fires on /batu-bolong-surf/, deploy ERROR reverted

**Day**: 121 (loop-run count)
**Previous action impact**: no claim to score. 09-21's action (`64b4ce8`, Rule 6 internal
linking on `/private-surf-lesson-bali/`) registered no ranking/click hypothesis — purely
structural, consistent with the 09-14/09-15 practice for link-only edits. Nothing to evaluate.

---

## GSC Snapshot (28-day window, 2026-08-23 → 2026-09-19)

Window rolled **+1 day** on both ends vs 09-21's window (2026-08-22 → 2026-09-18). Not
byte-identical to any prior log — a fresh firing, not a duplicate.

### Totals
- Clicks: **52** (Δ +1 vs 09-21's 51)
- Impressions: **4334** (Δ −64 vs 09-21's 4398)
- CTR: **1.2%** (Δ +0.04)
- Avg position: **13.5** (Δ −0.1, marginally better)

### Top pages (≥10 impressions)

| Page | Clicks | Impr | CTR | Pos |
|---|---|---|---|---|
| `/` | 15 | 140 | 10.7% | 11.2 |
| `/batu-bolong-surf/` | 10 | 1295 | 0.8% | 10.3 |
| `/blog/batu-bolong-beach-canggu-guide/` | 6 | 783 | 0.8% | 13.6 |
| `/blog/kids-surf-lessons-canggu/` | 4 | 16 | 25.0% | 12.3 |
| `/blog/intermediate-surf-coaching-canggu/` | 3 | 23 | 13.0% | 13.7 |
| `/blog/learn-to-surf-cant-swim-bali/` | 3 | 38 | 7.9% | 6.6 |
| `/blog/what-to-bring-surf-lesson-bali/` | 3 | 15 | 20.0% | 5.7 |
| `/blog/best-time-to-surf-canggu/` | 2 | 104 | 1.9% | 46.0 |
| `/blog/bali-surf-itinerary-7-days/` | 1 | 10 | 10.0% | 11.8 |
| `/blog/canggu-surf-forecast/` | 1 | 1109 | 0.1% | 9.7 |
| `/blog/how-long-to-learn-surfing-bali/` | 1 | 30 | 3.3% | 8.6 |
| `/blog/learn-to-surf-over-40-bali/` | 1 | 17 | 5.9% | 5.9 |
| `/blog/solo-surf-lessons-canggu/` | 1 | 16 | 6.3% | 37.0 |
| `/beginner-surf-lessons-bali/` | 0 | 22 | 0% | 36.0 |
| `/blog/` | 0 | 45 | 0% | 37.2 |
| `/blog/batu-bolong-surf-forecast/` | 0 | 12 | 0% | 6.1 |
| `/blog/canggu-surfing-safe-for-kids/` | 0 | 35 | 0% | 9.7 |
| `/blog/canggu-vs-uluwatu-beginners/` | 0 | 205 | 0% | 34.1 |
| `/blog/family-surf-lessons-bali/` | 0 | 18 | 0% | 8.9 |
| `/blog/private-vs-group-surf-lesson-bali/` | 0 | 20 | 0% | 17.9 |
| `/blog/surf-lessons-seminyak-kuta-legian/` | 0 | 111 | 0% | 28.0 |
| `/blog/when-to-book-surf-lessons-canggu/` | 0 | 52 | 0% | 5.9 |
| `/private-surf-lesson-bali/` | 0 | 12 | 0% | 3.8 |
| `/surf-lesson-prices-bali/` | 0 | 249 | 0% | 11.4 |

### Top queries (≥20 impressions, page-query join, 145-row `dimensions:['page','query']`,
temporary script written at repo root, run once, then deleted — same pattern as every run
since 09-08)

| Query | Clicks | Impr | Pos |
|---|---|---|---|
| batu bolong beach | 1 | 344 | 20.1 |
| msw canggu | 0 | 226 | 9.0 |
| surfline batu bolong | 0 | 103 | 7.0 |
| batu bolong surfline | — | 16 | 6.1 |
| magicseaweed batu bolong | 0 | 82 (78 on `/batu-bolong-surf/`) | 4.6–5.1 |
| batu bolong | 0 | 69 | 16.8 |
| batu bolong surf report | 0 | 60 | 6.9 |
| surf forecast batu bolong | 1 | 37 | 9.6 |
| uluwatu surf beginner | 0 | 54 | 39.3 |
| batu bolong surf forecast | 0 | 46 | 6.8 |
| canggu surf report | 0 | 51 | 25.7 |
| surfing in uluwatu for beginners | 0 | 41 | 38.7 |
| uluwatu surfing beginners | 0 | 38 | 37.3 |
| batu bolong beach canggu | 0 | 24 | 22.5 |
| batu bolong canggu | 0 | 21 | 20.1 |
| batu bolong beach bali | 0 | 20 | 11.3 |
| surf lessons seminyak | 0 | 20 | 49.6 |
| surfline canggu | 0 | 27 | 9.4 |

No new entrant/exit worth flagging vs 09-21 at this threshold; membership essentially
unchanged.

### `by_search_type`

image search continues to run near ~240 impr, 0 clicks (not re-verified in detail this run —
Rule 5 stays orthogonality-locked, see below).

---

## Ladder walk

```
ZASADA NADRZĘDNA (content rhythm) — ESCALATED, TICKET STILL UNANSWERED.
  Day 32 of the ticket (opened 2026-08-21), 39 days since the last loop-added MDX post
  (2026-08-14, surf-lessons-for-women-bali). No owner commit since the ticket opened
  (git log unchanged from 09-21's check). Per v4: descend the ladder, say so once, do not
  invent a fourth option. Done.

RULE 1 — MATCH. Band 10-20, impr≥10, re-derived from a fresh 145-row page-query join:

  | Page | Impr @ Pos | Verdict |
  |---|---|---|
  | / (homepage) | 140 @ 11.2 | excluded on the two non-expiring 09-08 grounds (no in-scope
      edit surface; click-trade against 15/52 site clicks) |
  | /batu-bolong-surf/ | 1295 @ 10.3 | **NEW ENTRANT — Read 8 control, lock EXPIRES TODAY** |
  | /blog/batu-bolong-beach-canggu-guide/ | 783 @ 13.6 | Read 8 treatment arm, lock also
      expires today, but see below — not chosen |
  | /blog/kids-surf-lessons-canggu/ | 16 @ 12.3 | query-anonymous — zero join rows |
  | /blog/intermediate-surf-coaching-canggu/ | 23 @ 13.7 | named query "intermediate surf
      lessons" 2 impr @ 18.5 — below the ≥3-impr visibility floor, unchanged for 5+ windows |
  | /blog/bali-surf-itinerary-7-days/ | 10 @ 11.8 | query-anonymous — zero join rows |
  | /surf-lesson-prices-bali/ | 249 @ 11.4 | rejected by v4 popr. 9 ("named and still losing",
      unchanged) |
  | /blog/private-vs-group-surf-lesson-bali/ | 20 @ 17.9 | named query "group surf lessons"
      4 impr @ 52.3 (byte-identical since 09-13) — "named and still losing" verdict stands |

  **Read 8 closes today, indeterminate, both arms unlock regardless (pre-committed 09-11 and
  reaffirmed 09-21).** Mean of the 3 registered readings for the control `/batu-bolong-surf/`:
  09-16 = 12 cl, 09-19 = 12 cl, **09-22 (today) = 10 cl → mean 11.33**. Registration bands:
  ≤10.0 fires the cannibalisation-diagnostic branch, ≥13.0 closes "separated cleanly"; 11.33
  falls in the untested middle, which the original registration never assigned a verdict to
  either (see 09-09's first Read 8 closure, same gap). Since this is the last scheduled
  reading, it closes **NO VERDICT — genuinely indeterminate**, not re-registered (no bar was
  written in advance for a follow-up, and writing one now would be exactly the read-after-write
  error the loop has refused before). The share qualifier, however, kept rising every check
  (0.585 → … → 0.618 → **0.623 today**) and never came close to failing — recorded for the
  record: whatever else the mean says, the control was never observed losing exposure share to
  the post.

  Both pages are now open. Tie-breakers (playbook, in order): (1) both are already-ranked,
  non-zero-impression pages — tied; (2) impressions — **`/batu-bolong-surf/` wins outright**,
  1295 vs 783; (4) never-been-a-Rule-1-target — also tied (neither has). **Chosen:
  `/batu-bolong-surf/`** — largest page ever to clear the Rule 1 band in this loop's history.

  **Query-visibility gate**: clears easily. Named queries on this page from the fresh join,
  all with real position already inside page 1: `surfline batu bolong` (103 impr @ 7.0, 0 cl),
  `magicseaweed batu bolong` (78 @ 4.6), `batu bolong surf report` (59 @ 6.1), `batu bolong
  surf forecast` (46 @ 6.8), `surf forecast batu bolong` (36 @ 7.6, 1 cl), `batu bolong
  surfline` (16 @ 6.1). The page's *average* position (10.3) sits inside the Rule 1 band only
  because ~85 impressions of unrelated long-tail (`batu bolong beach` @ 69.8 among them) drag
  the mean down — the named forecast-brand queries are themselves already ranking well but not
  converting, which is exactly Rule 1's "Google doesn't consider the page relevant enough for
  *this* query" framing once you separate query from page average. Not rerouted to Rule 4:
  page-level CTR is 0.77% (<2%, would also qualify), but Krok 0's own table keys off page
  position (10.3, not 5–10) and page clicks are non-zero (10, not the "0 kliknięć" symptom), so
  Rule 1 is the correct instrument per the playbook's own diagnostic table.

  **Third-exit check ("named and still losing")**: does not apply — `surfline batu bolong` has
  never appeared in this page's title, meta, any H2 or lede before today. Fresh candidate.

  **Precedent for the lever**: the sibling page `/blog/canggu-surf-forecast/` already targets
  the analogous brand-navigational query `msw canggu` (226 impr, its own top query) in its
  title ("Canggu Surf Report & Forecast 2026: MSW, Surfline, Spots"). Naming a forecast-tool
  brand for a forecast-intent page is an established pattern on this site, not a new risk.

  RULE 1 FIRES. Ladder does not reach Rule 1-B through Rule 8 today.
```

## Action chosen

```
ACTION: Rule 1 — striking-distance naming, narrow don't add
TARGET: src/app/batu-bolong-surf/page.tsx
RATIONALE: Page entered the 10-20 band today as its Read 8 orthogonality lock expired
        (indeterminate close, unlocks regardless per the 09-11 registration). Largest page
        ever to clear the Rule 1 band (1295 impr). Query-visibility gate clears on
        "surfline batu bolong" (103 impr @ pos 7.0, largest named query, never targeted
        before). Precedent: /blog/canggu-surf-forecast/ already names "MSW" and "Surfline"
        in its own title for the same brand-navigational pattern.
HYPOTHESIS: title/meta/H2/lede naming the exact phrase "surfline batu bolong" should improve
        that query's already-good position (7.0) into clicks, and should not move the page's
        long-tail-diluted average position much on its own — read in ~1-2 windows once live.
```

## Implementation

Files changed (1 file):
- `src/app/batu-bolong-surf/page.tsx` — title, meta description, keywords array, one H2
  ("Batu Bolong Surf Report & Live Conditions" → "Surfline Batu Bolong: Surf Report & Live
  Conditions") and the opening lede of that section rewritten to contain the exact phrase
  "Surfline Batu Bolong". No section added, nothing removed, no other page touched.

`npm run build` passed locally (46 static routes) before commit.

**Commit 1 (code)**: `b40d12a` — pushed, then **Vercel build ERRORED**
(`errorCode: BUILD_UTILS_SPAWN_1`, "Command \"npm run build\" exited with 1"). Build log:
Turbopack failed to resolve `next/font/google` (DM Sans) — "Module not found: Can't resolve
'@vercel/turbopack-next/internal/font/google/font'" inside `src/app/layout.tsx`'s font import,
a file this commit never touched.

**Root-caused, not assumed**: reproduced locally. With a warm `.next` build cache (left over
from this run's earlier successful local build) `npm run build` failed **with the exact same
error**, on the already-reverted, pre-existing codebase — i.e. the failure is unrelated to
this commit's content. `rm -rf .next && npm run build` on the same code then succeeded
cleanly. Vercel's own build log for the failed deployment shows "Restored build cache from
previous deployment" — a stale Turbopack font-resolution cache entry, not a code defect.

**Per Step 9's ERROR protocol**: reverted. `git revert HEAD --no-edit` → **`b9ddb01`**, pushed.
Deployment `dpl_CVw8aY94wMjHNKqvKZmJbJfaggEd` — **READY** (build ~27s, fresh cache, no font
error), aliased to `surfingwithrocky.com`. Production was never at risk: the ERRORed deployment
was never promoted to the alias (Vercel doesn't promote a failed build), so the site was
serving the prior good deployment (`124097a`) throughout.

Smoke test: `WebFetch` on `surfingwithrocky.com` returned `EGRESS_BLOCKED` (consistent with
every run since 09-14); fell back to `mcp__Vercel__web_fetch_vercel_url` on the revert
deployment's own `.vercel.app` URL — confirms the live page correctly serves the **original**
title (`Batu Bolong Surf Report & Beginner Guide | Canggu, Bali`) and H2s, i.e. the revert is
verified live and the site is healthy.

**Net effect on production today: none — the SEO change did not ship.** The content decision
itself is sound and untouched by the infra failure; it is a ready-to-reapply candidate, not a
rejected one.

## Notes for tomorrow

1. **Re-attempt the same Rule 1 action on `/batu-bolong-surf/` tomorrow if it still clears the
   ladder.** Today's failure was infrastructure (a stale Vercel/Turbopack font-resolution build
   cache), not a content or code defect — confirmed by local reproduction on the *unmodified*
   codebase and by a clean build immediately after clearing `.next`. This is not a "named and
   still losing" case; `surfline batu bolong` has still never shipped. Do not let this read as
   evidence against the query choice.
2. **If another run hits the same `next/font/google` / Turbopack build error**, root-cause it
   the same way before reverting on reflex: rebuild locally with a warm cache first (reproduces
   the flake), then `rm -rf .next` and rebuild clean (confirms it's cache, not code). Two
   occurrences would make this a standing infra issue worth a note to the owner — `next.config.ts`
   and Vercel project settings are both outside the loop's edit scope.
3. **Read 8 is now closed, indeterminate** (mean 11.33 clicks, outside both the ≤10.0 and
   ≥13.0 branches). Not re-opened. The practical upshot: `/batu-bolong-surf/` and
   `/blog/batu-bolong-beach-canggu-guide/` are both now permanently unlocked from this specific
   orthogonality hold and available to any rule on their own current-day merits — no more
   "Read 8 control/arm" exclusion line needed in future ladder walks.
4. Content escalation (Standing Failure 1) unchanged: day 32, 39 days since the last MDX post,
   ticket still unanswered. Options unchanged: (a) keyword research outside GSC, (b) lift the
   editorial ban on the Uluwatu pool, (c) approve a post without data support.
5. Rule 6/7's shared weekly budget: unchanged today (Rule 1 fired before reaching Rule 6),
   still 1/2 spent for the week beginning 09-21.
6. Page↔query join still absent from `scripts/gsc-snapshot.mjs` — carried a **twentieth** run,
   outside the loop's edit scope, needs the owner.
7. Two commits landed today instead of the usual one code + one log: `b40d12a` (the attempted
   fix) and `b9ddb01` (its mandated revert per Step 9's ERROR protocol) both count as the run's
   single "code" action — a failure-recovery pair, not a second daily action.

---

## Addendum

None — single firing today (Rule 1 attempted, reverted after deploy ERROR; see above).
