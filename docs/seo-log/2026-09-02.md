# 2026-09-02 — clicks take a new ATH on a genuinely accelerating rate, the Uluwatu guard expires on its own terms, and a page↔query join finally makes the visibility gate testable

**Day**: 101 (loop-run count)
**Action**: **Rule 6 #1 (Technical Polish)** — `BreadcrumbList` JSON-LD on `/blog/`, the last page on the site without one
**Previous action impact**: **pending, zero exposure days** (the 08-31 canonical sweep shipped after this window closed; its first read is 09-14)

Window rolled **+2 days** on both ends: 08-01→08-28 becomes **08-03→08-30**.

---

## THE HEADLINE: 45 clicks is a new all-time high, and for the first time this month the roll does *not* explain it away

| Metric | 08-31 | **Today** | Δ |
|---|---|---|---|
| Clicks | 43 | **45** | **+2 — NEW ATH** (previous 43, set 08-26 and 08-31) |
| Impressions | 5036 | **5064** | **+28 — 13th consecutive all-time high** |
| CTR | 0.85% | **0.89%** | +0.04 |
| Avg position | 13.4 | **13.7** | +0.3 adverse (26th window above 10) |

**Roll accounting first, before the win is claimed** — the discipline every log this
month has kept. Two days entered: **08-29 (1 click, 159 impr)** and **08-30 (4 clicks,
146 impr)**, five clicks between them. Net +2 therefore means the two departing days
(08-01, 08-02) carried **3**.

**What is different today is the underlying rate, which is the number the last four logs
have said was flat.** It is no longer flat:

| Trailing 7 days | Clicks | Rate |
|---|---|---|
| 08-17 → 08-23 | 9 | 1.29/day |
| 08-22 → 08-28 (yesterday's figure) | 10 | 1.43/day |
| **08-24 → 08-30** | **14** | **2.00/day** |

That is a **+40% move in the trailing rate against yesterday's own measurement**, computed
the same way, and it is not a roll artefact — a trailing-7 rate has no roll. The single
biggest contributor is **08-30: 4 clicks on 146 impressions = 2.7% CTR, the best
single-day CTR in the loop's recorded history** (previous best 2.4% on 08-21).

**The honest limit on that claim**: 14 clicks over 7 days is a small count, and one
4-click day carries 29% of it. Two more windows of ≥1.8/day would make this a trend;
one window makes it the first non-flat reading since 08-20. It is recorded as the
latter.

| | 08-31 | **Today** |
|---|---|---|
| Converting pages | 15 | **16** |
| Converting countries | 14 | **14** (IDN 23, SGP 5, GBR 3, AUS 2, DEU 2, MYS 2, then 1 each × 8) |
| Named visibility | 19% (8 of 43) | **17.8% (8 of 45)** |
| Device gap | 1.83× | **1.81×** — mobile 28→**29**, desktop 15→**16** |

**Both devices gained a click again**, a second consecutive window in which the gap
narrowed by addition rather than subtraction. Desktop still ranks 20.3 against
mobile's 7.7.

**Named visibility composition is unchanged**: 7 of 8 named clicks are Batu Bolong forms
(`batu bolong beach` 2, `batu bolong surf report` 2, `batu bolong` 1, `surf forecast batu
bolong` 1, `surfing batu bolong` 1); the 8th is `surf school near me` (1 impr @ 20.0).
**37 of 45 clicks (82%) still come from queries GSC will not name.**

---

## NEW THIS RUN: a page↔query join, and the first testable application of Rule 1's visibility gate

`scripts/gsc-snapshot.mjs` requests `['query']` and `['page']` as **separate** dimensions,
so every previous run has had to *infer* which page owns which query from the site-wide
list. Rule 1's query-visibility gate (v3, gate #2) and Rule 2's ownership audit (v3, Krok 0)
both depend on exactly that mapping, and both have been resolved by inference for 23 runs.

This run issued **one additional read-only query** against the same credentials and window
with `dimensions: ['page','query']` — **152 rows**. It is a diagnostic, not a repo change;
nothing under `scripts/` was modified (out of the allowed scope).

**What it changes, concretely:**

| Finding | Previously | Now |
|---|---|---|
| `/blog/when-to-book-surf-lessons-canggu/` | assumed query-anonymous because no "when to book" query appears in the site list | **confirmed: appears in ZERO of 152 page↔query rows.** All 34 impressions anonymised |
| `/blog/how-long-to-learn-surfing-bali/` | same inference | **confirmed: zero rows** |
| `/blog/canggu-surf-forecast/` brand-nav share | estimated "~495 of 898" from site-wide totals | **measured: 190 of its 268 named impressions = 71%** (`msw canggu` 162, `magicseaweed batu bolong` 18, `surfline canggu` 6, `canggu surfline` 4) |
| `/` homepage | never evaluated for Rule 1 | **named for `batu bolong surf lessons` 9 @ 9.3, `surf trainer` 4 @ 7.3, `batu bolong surf school` 2 @ 9.5** — it *passes* the visibility gate |
| `/batu-bolong-surf/` vs the 08-03 post | control assumed clean | **both rank for `batu bolong beach`: the post at 11.9 with 409 impr, the control at 69.0 with 12 impr.** Google has resolved the split decisively in the post's favour |
| `/blog/private-vs-group-surf-lesson-bali/` | 20 impr, unexplained | ranks for **`laguna coast surf school` (pos 6)** and **`pillar point surf school` (pos 4)** — *California* surf schools. Its named impressions are geographic noise |

The Batu Bolong row is worth stating plainly because it is the first **direct** evidence on
the 13-window cannibalisation question rather than inference from aggregates: the two pages
do compete on the core query, and the control loses it at position 69 while the post holds
11.9. That is Google choosing, not the site splitting — and it is consistent with the
control's own position being untouched at 8.9.

**Recommendation for the owner / next META run** (cannot be done from inside the loop —
`scripts/` is outside the allowed edit scope): add a `page_query` block to
`scripts/gsc-snapshot.mjs`. It is one extra `safeQuery` call and it retires the single
largest source of inference in the whole playbook.

---

## READS

### Read 5 (the 08-21 revert guard on `/blog/canggu-vs-uluwatu-beginners/`): **THE GUARD EXPIRES — condition met exactly as pre-registered**

| | at narrowing | 08-28 | 08-29 | 08-31 | **Today** |
|---|---|---|---|---|---|
| Page | 271 @ 36.9, 2 cl | 279 @ 40.2, 2 cl | 280 @ 40.4, 1 cl | 278 @ 40.2, 1 cl | **282 @ 40.1, 1 cl** |

Yesterday's pre-registration, written before this number was seen:

> *"If the next window reads 1 click with position within ±1.5 of 40.2, the guard EXPIRES
> and the page is closed as failed-but-stable. 0 clicks still fires it; 2 still closes it
> clean."*

It reads **1 click at 40.1**. |40.1 − 40.2| = 0.1, well inside ±1.5. **The guard expires.
The page is CLOSED as failed-but-stable. No revert.** Position has now moved
40.4 → 40.2 → 40.1, three consecutive non-adverse readings.

This is the third pre-registered condition this week to resolve on the exact terms it was
written in, and the first to close a read that had been returning an unresolvable middle
value. The 185-impression Uluwatu pool (`uluwatu surf beginner` 64 @ 44.9, `surfing in
uluwatu for beginners` 58 @ 51.4, `uluwatu surfing beginners` 58 @ 44.0, plus three smaller
forms — now **measured** rather than inferred) remains unserved and routes to ticket
option (b).

### Read 8 (rolling, 15th window) — the 08-03 post and its cannibalisation control: **the pre-registered condition does NOT fire**

| | 08-29 | 08-31 | **Today** |
|---|---|---|---|
| `/blog/batu-bolong-beach-canggu-guide/` | 1159 @ 12.0, 4 cl | 1228 @ 12.3, 4 cl | **1284 @ 12.4, 4 cl** |
| `batu bolong beach` | 392 @ 13.3, 2 cl | 412 @ 13.7, 2 cl | **421 @ 13.6, 2 cl** |
| `batu bolong` | 195 @ 10.1, 1 cl | 207 @ 10.0, 1 cl | **216 @ 10.1, 1 cl** |
| **control** `/batu-bolong-surf/` | 1639 @ 8.6, 14 cl | 1669 @ 8.8, **12 cl** | **1625 @ 8.9, 13 cl** |

Yesterday's pre-registration: *"if the control reads **≤12 clicks again** next window with
impressions ≥1600, that is a third consecutive non-improving window and it earns a
**dedicated diagnostic run**."*

It reads **13 clicks on 1625 impressions**. 13 > 12. **The condition does not fire. No
diagnostic run is scheduled.** The two-window slide (15 → 14 → 12) has reversed by one.

The counterweight, stated because yesterday's log leaned the other way: the control's
position is unchanged at 8.9 and its impressions *fell* 44 while its clicks rose 1 — so
the recovery is a CTR move on a shrinking base, not a rank move. Combined with the new
page↔query finding above (the control ranks 69.0 for `batu bolong beach` against the
post's 11.9), the most defensible reading after 15 windows is that **the two pages have
separated cleanly rather than cannibalised**, and the click wobble is noise on a
13–15 click base. The read stays open; no verdict is upgraded on one window.

### Read 1 (registered 08-28, deadline 09-03 — tomorrow) — CTR rescue on `/blog/learn-to-surf-cant-swim-bali/`: **PENDING, 2 exposure days**

| | at strike (08-28) | 08-29 | 08-31 | **Today** |
|---|---|---|---|---|
| Page | 28 impr @ 8.2, 0 cl | 30 @ 8.1, 0 cl | 32 @ 8.0, 0 cl | **32 @ 7.0, 0 cl** |

**Position guard: baseline 8.2, registered band ±1.5 → 6.7–9.7. Today reads 7.0, inside**
(and improving by 1.2 since the strike). Two exposure days of the fourteen. **Reads
tomorrow, 09-03**, and it should be read against the same power arithmetic the 08-29 run
introduced: 32 impressions per 28 days is 1.14/day, so two exposure days is ~2 impressions.
**A null tomorrow carries essentially no information and must not be scored as a Rule 4
failure.**

### Read 2 (registered 08-29, deadline 09-12) — CTR rescue on `/blog/family-surf-lessons-bali/`: **PENDING, 1 exposure day**

| | at strike (08-29) | 08-31 | **Today** |
|---|---|---|---|
| Page | 26 impr @ 11.1, 0 cl | 22 @ 11.6, 0 cl | **22 @ 12.0, 0 cl** |

Position guard (baseline 11.1, VOID if worse than 12.6): **12.0, inside — but it has now
drifted 0.9 of its 1.5 allowance in three windows and is trending toward the void
boundary.** Power guard unchanged and still tightening: 22 impr / 28 days = 0.79/day,
so fourteen exposure days is ~11 impressions and predicted clicks at position ~12 remain
**~0.11**. **Only a click carries information; a null on 09-12 reads UNDERPOWERED.**

### Read 3 (registered 08-26, deadline 09-09) — the filename sweep: **pending, expected-null**

`by_search_type` returns **`web` only** for a **fifth** consecutive window; image
impressions remain **zero** against 15 image URLs submitted. Expected state at 7 days,
not a result.

### Read 4 (rolling, 6 exposure days) — the 08-25 homepage internal links

| Target | 08-29 | 08-31 | **Today** |
|---|---|---|---|
| `batu-bolong-beach-canggu-guide` | 1159 @ 12.0, 4 cl | 1228 @ 12.3, 4 cl | **1284 @ 12.4, 4 cl** |
| `surf-camp-vs-surf-lessons-bali` | 10 @ 7.5, 0 cl | 8 @ 7.4, 0 cl | **8 @ 7.4, 0 cl** |
| `surf-lessons-for-women-bali` | 2 @ 12.5 | 4 @ 10.3 | **4 @ 10.3** |
| `do-you-need-to-be-fit-to-surf-bali` | 2 @ 3.0 | 2 @ 3.0 | **3 @ 5.0** |

`surf-lessons-for-women-bali` **held** its one-window gain (4 impr @ 10.3) rather than
giving it back; `do-you-need-to-be-fit-to-surf-bali` moved for the first time in five
windows (2 → 3 impr). Both are far too small to reopen the 08-28 Priority-2 verdict and
neither is claimed to. Recorded because the 08-25 log flagged that both Priority-2 tests
shipped without inbound link support, which confounds the evidence v4 poprawka 8 rests on.

### Read 6 (the 08-15 naming strike on `/blog/canggu-surf-forecast/`, closed failed 08-26): **the adverse drift continues**

| | at strike | 08-29 | 08-31 | **Today** |
|---|---|---|---|---|
| `canggu surf report` | 55 @ 22.7 | 49 @ 25.8 | 52 @ 26.3 | **53 @ 26.7** |
| Page | 1017 @ 9.9 | 900 @ 10.7, 1 cl | 898 @ 10.6, 1 cl | **915 @ 10.7, 1 cl** |

Another **0.4 adverse** on the target query, on rising impressions. Verdict unchanged —
**failed**, 22.7 → 26.7 total drift across the whole read. The page-level join now
quantifies why the page cannot be rescued: **71% of its named impressions are
brand-navigational**, ranking 4.1–9.7 and returning zero clicks. People typing `msw canggu`
want magicseaweed.com.

### Read 7 (the 08-16 naming strike on `/blog/intermediate-surf-coaching-canggu/`): **partial recovery of yesterday's break**

| | at strike | 08-28 | 08-29 | 08-31 | **Today** |
|---|---|---|---|---|---|
| `intermediate surf lessons` | 3 @ 22.3 | 4 @ 20.5 | 4 @ 20.5 | 4 @ 20.5 | **4 @ 20.5** |
| Page | 14 @ 16.3, 0 cl | 21 @ 16.8, 1 cl | 22 @ 16.0, 2 cl | 22 @ 19.0, 2 cl | **22 @ 18.6, 2 cl** |

The 3.0-position give-back reported yesterday recovers **0.4** (19.0 → 18.6). The query is
unmoved at 20.5 for a **fourth** consecutive window. Yesterday's framing stands without
change: on a 22-impression page these swings are inside noise, and v3 Amendment 1's only
positive instance remains unstable at **1–1** across two closed reads.

---

## Action chosen

```
LADDER WALK (every step re-derived from today's window; the visibility gate is
checked against the 152-row page-query join, not inferred, for the first time):

ZASADA NADRZEDNA (content rhythm) — ESCALATED, TICKET UNANSWERED (12 days).
  Verified, not assumed: `git log --since=2026-08-19 -- docs/` returns six
  commits, all this loop's own `seo(log):` entries; no owner reply exists in
  docs/. v4: a run finding the ticket unanswered "nie wymysla czwartej opcji
  i nie wraca do pisania postow — schodzi drabina nizej". Done; the run descends.
  Days since last MDX post: 19 (08-14). Conversions in a row: 2/3, unchanged.
  NOTE: the 21-day mark falls on 2026-09-04 and v4 still has no rule for it.

RULE 1 — NO MATCH (7th consecutive run). Band 10-20 with >=10 impr:
  | Page                                    | impr @ pos  | Named query >=3 impr?      | Verdict |
  | /blog/batu-bolong-beach-canggu-guide/   | 1284 @ 12.4 | yes (batu bolong beach 409)| EXCLUDED — live 08-03 experiment + 08-25 link target |
  | /blog/canggu-surf-forecast/             |  915 @ 10.7 | yes (msw canggu 162)       | "NAMED AND STILL LOSING" (v4 popr. 9) — closed failed 08-26, drifted again today |
  | /surf-lesson-prices-bali/               |  276 @ 15.4 | yes (surfing lessons bali price 16) | REJECTED by v4 popr. 9 — the rule's own source case |
  | / (homepage)                            |  121 @ 11.1 | YES — batu bolong surf lessons 9 @ 9.3 | PASSES the gate for the first time, but EXCLUDED: title/description/canonical live in src/app/layout.tsx (FORBIDDEN FILE) and H1/lede/anchors live in Hero/About/Lessons components, none of which are in the allowed edit scope. Rule 1's action has NO editable surface here. |
  | /blog/how-long-to-learn-surfing-bali/   |   38 @ 10.9 | NO — zero page-query rows  | QUERY-ANONYMOUS (now confirmed, not inferred) |
  | /blog/family-surf-lessons-bali/         |   22 @ 12.0 | marginal                   | LOCKED — struck 08-29, read open to 09-12 |
  | /blog/intermediate-surf-coaching-canggu/|   22 @ 18.6 | yes (intermediate surf lessons 4) | NO ACTION LEFT — naming fully applied 08-16; open read |
  | /blog/batu-bolong-surf-forecast/        |   10 @ 16.2 | NO — zero rows             | EXCLUDED — control-contaminating (08-03 cluster) AND anonymous |
  ( /blog/solo-surf-lessons-canggu/ 13 @ 28.0 and /blog/private-vs-group-surf-
    lesson-bali/ 20 @ 21.9 are both OUT OF BAND (>20). /blog/learn-to-surf-
    cant-swim-bali/ 32 @ 7.0, /blog/when-to-book-surf-lessons-canggu/ 34 @ 8.9,
    /blog/canggu-surfing-safe-for-kids/ 47 @ 9.2, /blog/kids-surf-lessons-
    canggu/ 33 @ 9.7 and /surf-lessons-canggu 12 @ 8.8 are all OUT OF BAND (<10). )

RULE 2 — BLOCKED by the escalation. Ownership audit deliberately NOT re-run:
  it would be the 8th consecutive failure and v4 forbids substituting a
  fourth option while the ticket stands.

RULE 3 — NO MATCH, and for the first time verified at page level rather than
  by inference. Every query at >=5 impressions has an owner in the join:
  Batu Bolong place-names ~835 (08-03 post), brand-nav msw/surfline/msw-berawa
  ~495 (unconvertible), Uluwatu 185 (editorial ban; 1-B failed 08-28, page
  closed today), `canggu surf report` 53, Seminyak ~51 (08-06 post),
  `surfing lessons bali price` 16 (prices page), `batu bolong surf lessons` 9
  (homepage), `group surf lessons` 4 and `intermediate surf lessons` 4 (owned).
  Largest genuinely un-owned intent is still `berawa beach surfing`, 1 impr.

RULE 4 — MATCHES ON ENTRY, BRAKED BY ITS OWN GUARD. See below.

RULE 5 — NO MATCH, both branches checked today rather than assumed:
  (a) `by_search_type` returns web only; image impressions ZERO for a 5th
      consecutive window, so the ">=100 image impressions" branch fails;
  (b) every <Image>/<img> in src/ carries alt text and the 08-26 sweep left
      no uninformative filenames.
  Also checked and correctly NOT fired: Rule 6 #5 (VideoObject). The site's
  only video is public/videos/hero.mp4, rendered in Hero.tsx as
  autoplay+loop+muted+playsInline with no controls and hidden below md.
  That is a DECORATIVE BACKGROUND video, which Google's structured-data
  guidelines explicitly exclude from VideoObject. Marking it up would be
  schema spam, not Image/Video SEO.

RULE 6 — MATCH (#1, BreadcrumbList). Weekly budget: 1/2 spent (08-31 sweep),
  so one slot remains. Mechanical sweep 1/1, SPENT — today's action is a
  single-file additive change and is explicitly NOT a sweep.
```

**Rule 4: the entry condition matches, and the guard brakes it. Both halves shown.**

| Page | impr @ pos | CTR | Verdict |
|---|---|---|---|
| `/batu-bolong-surf/` | 1625 @ 8.9 | 0.8% | EXCLUDED — the cannibalisation **CONTROL**, 15 live windows |
| `/blog/batu-bolong-beach-canggu-guide/` | 1284 @ 12.4 | 0.3% | EXCLUDED — the 08-03 experiment itself |
| `/blog/canggu-surf-forecast/` | 915 @ 10.7 | 0.1% | EXCLUDED — two failed rescues; guard check performed and recorded 08-10 |
| `/blog/canggu-vs-uluwatu-beginners/` | 282 @ 40.1 | 0.4% | too deep (page 4); on-page work ended 08-28, page closed today |
| `/surf-lesson-prices-bali/` | 276 @ 15.4 | 0% | snippet already correct (verified 08-29) |
| `/blog/surf-lessons-seminyak-kuta-legian/` | 143 @ 27.8 | 0% | too deep |
| `/` | 121 @ 11.1 | **8.3%** | CTR not < 2% — the site's best-converting page |
| `/blog/` | 56 @ 45.6 | 0% | too deep for a *snippet* fix — but see the action below |
| `/blog/canggu-surfing-safe-for-kids/` | 47 @ 9.2 | **2.1%** | CTR not < 2% |
| `/blog/how-long-to-learn-surfing-bali/` | 38 @ 10.9 | **2.6%** | CTR not < 2% |
| **`/blog/when-to-book-surf-lessons-canggu/`** | **34 @ 8.9** | **0%** | **lock expired TODAY — the only candidate. Braked; see below** |
| `/blog/kids-surf-lessons-canggu/` | 33 @ 9.7 | **12.1%** | CTR not < 2% — best on the site |
| `/blog/learn-to-surf-cant-swim-bali/` | 32 @ 7.0 | 0% | LOCKED — read 09-03 |
| `/beginner-surf-lessons-bali/` | 31 @ 32.2 | 0% | too deep |
| `/blog/family-surf-lessons-bali/` | 22 @ 12.0 | 0% | LOCKED — read 09-12 |
| `/blog/intermediate-surf-coaching-canggu/` | 22 @ 18.6 | **9.1%** | CTR not < 2% |
| `/blog/private-vs-group-surf-lesson-bali/` | 20 @ 21.9 | 0% | too deep (page 3); named queries are California noise |
| `/blog/learn-to-surf-over-40-bali/` | 20 @ 6.4 | **5.0%** | CTR not < 2% |

**`/blog/when-to-book-surf-lessons-canggu/` clears every literal gate**: 34 impressions
(≥20), 0% CTR (<2%), and its 14-day lock from the 08-19 rescue expired today — the date
three prior logs (08-28, 08-29, 08-31) all named in advance as **09-02**. It was not
waved past. Rule 4's own re-rescue guard applies verbatim:

> *"jeśli strona miała CTR rescue i po 14 dniach dalej 0% CTR — sprawdź Google Cache
> (czy nowy title w ogóle się zaindeksował) zanim zrobisz kolejny rescue."*

**The check was performed today, and its instrument is named honestly.** Google Cache
was retired as a product, so the guard's literal tool no longer exists. What was done
instead:

- **Live fetch of production**: `200`, `<title>` served as *"When to Book Surf Lessons in
  Canggu: 6:30 AM, 5 Days Ahead"* and the 145-char description exactly as shipped on
  08-19. The snippet is live and unchanged.
- **Evidence Google has re-crawled since**: the page has drawn impressions continuously
  for 13 days and its position has been actively re-ranked across four windows
  (8.9 → 10.1 → 9.8 → **8.9**). A page Google re-ranks weekly has re-read a `<title>` from
  14 days ago.

**The guard's check therefore answers: the title indexed, and the CTR did not move.**
That is the same finding recorded for `/blog/canggu-surf-forecast/` on 08-10, and both
branches of the guard point the same way — a title that never indexed means the first
rescue is still untested, and a title that did index means the first rescue was tested
and failed. **Neither branch licenses an immediate second rewrite.**

**Three further facts, each of which independently would have stopped this action:**

1. **The page is query-anonymous — now confirmed rather than inferred.** It appears in
   **zero of 152 page↔query rows**. A CTR rescue rewrites a snippet to answer a query;
   there is no query to answer. The 08-19 rescue guessed a payload ("6:30 AM, 5 Days
   Ahead") and it did not convert.
2. **The read would be underpowered by ~4×.** 34 impressions / 28 days = 1.21/day, so
   fourteen exposure days is ~17 impressions. At position 8.9 an ordinary 2% CTR predicts
   **0.34 clicks**. That is below the ≥70-impressions/window bar the 08-29 run proposed and
   well inside the trap the 08-28 and 08-29 strikes already walked into. **Re-running a
   test at a power level where it cannot produce a readable result is not a fix.**
3. The one thing that argued *for* proceeding is recorded rather than suppressed: the
   08-26 log named a **protocol defect** in this page's read — its position slid 8.9 → 10.1
   (>±1.0) mid-test — and proposed for v5 that such a read be **void**, not failed, adding
   *"Under that rule 08-19 would be void."* Today the position has returned to **exactly
   8.9**, so that confound has cleared and a clean re-test is genuinely available. **It was
   not taken, because (2) says the clean re-test would still be unreadable, and because
   acting on a v5 amendment that was deliberately never written is precisely the
   write-before-read error v3 exists to prevent.**

```
ACTION: Rule 6 #1 (Technical Polish) — BreadcrumbList JSON-LD on the blog index
TARGET: src/app/blog/page.tsx (1 file, +23 lines, additive only)
RATIONALE: an audit of all 43 page files found exactly two without a
        BreadcrumbList: src/app/blog/page.tsx and src/app/page.tsx. The
        homepage is the site root, where a one-item trail is meaningless and
        Google ignores it — so /blog/ is the ONLY real instance left, and
        Rule 6 #1 is the top of Rule 6's own priority list. Rule 6 #2 (FAQPage
        on a page with FAQs but no schema) returned zero candidates; #3
        (internal links) has no orphans left — the weakest-linked page is the
        08-03 experiment, which orthogonality protects; #4 (alt text) is clean;
        #5 (VideoObject) correctly does not fire on a decorative background
        video. Item URLs use the trailing-slash form, agreeing with the
        canonical, the 08-31 sweep and all 43 sitemap <loc> entries.
HYPOTHESIS: NO ranking or click claim is made, and none would be credible —
        /blog/ draws 56 impressions at position 45.6 with 0 clicks. The
        measurable prediction is ENHANCEMENT COVERAGE: by 2026-09-16 the GSC
        Breadcrumbs enhancement report should list 43 valid items rather than
        42, and /blog/ should validate in the Rich Results test. This is a
        completeness fix, recorded as such rather than dressed up as growth.
RISK, STATED: none material. The change is additive, touches no user-visible
        text, no title, no description and no canonical; the blog index and
        every post were smoke-tested after deploy.
```

**Why a low-value action was preferred to an available one.** Rule 4 offered a page that
passes every literal gate, and taking it would have produced a more impressive-looking
run. It was declined because the arithmetic says the result would be unreadable and the
rule's own guard says the experiment has already been run. **The loop's failure mode has
never been doing too little on a given day — it is manufacturing a readable-sounding
action out of an unreadable one.** A completed structural fix that never needs revisiting
is worth more than a snippet rewrite whose null was predictable before it shipped.

---

## Implementation

**Files changed (1)** — `src/app/blog/page.tsx`, +23 lines, 0 deletions:

- module-level `BREADCRUMB_JSON_LD` const, matching the idiom already used in
  `src/app/surf-lessons-canggu/page.tsx` and the other landing pages
- one `<script type="application/ld+json" dangerouslySetInnerHTML={...} />` as the first
  child of the page root, matching the placement used in `src/app/page.tsx`
- trail: **Home** (`https://surfingwithrocky.com/`) → **Surf Blog**
  (`https://surfingwithrocky.com/blog/`)

Nothing else was touched. The existing `openGraph.url` on this file is still the
slash-less `${SITE_URL}/blog` — deliberately left alone, because it belongs to the
separate 42-file `og:url` mismatch named as a follow-up in the 08-31 log and fixing one
instance of it here would be an unlogged partial sweep.

`npm run build` — **passed**, all 43 routes prerendered; breadcrumb verified present in
`.next/server/app/blog.html` before commit.

**Commit**: `f92f5fa`
**Deployment**: `dpl_ECexg9nAFjaQVEuTiTJthgXP6cXh` — **READY** (production, aliased to
`surfingwithrocky.com`), commit SHA `f92f5fa89d33387d4729fb99f5cd5d2c2d577d07`

**Smoke test: PASS**

| Check | Result |
|---|---|
| `GET /blog/` | **200** |
| BreadcrumbList served | ✓ `{"@type":"BreadcrumbList","itemListElement":[{…"Home","item":"https://surfingwithrocky.com/"},{…"Surf Blog","item":"https://surfingwithrocky.com/blog/"}]}` |
| **Control** — `/blog/` canonical | `https://surfingwithrocky.com/blog/` — **unchanged** |
| **Control** — `/blog/` `<title>` | `Surf Blog — Tips & Guides for Beginners \| Surfing With Rocky` — **unchanged** |
| **Regression** — a post's own breadcrumb | `/blog/best-time-to-surf-canggu/` still carries its BreadcrumbList |

---

## Notes for tomorrow

1. **Read 1 (`learn-to-surf-cant-swim-bali`) is DUE 09-03.** Position guard is inside band
   and improving (8.2 → 7.0). **Two exposure days ≈ 2 impressions — a null tomorrow is
   arithmetically meaningless and must be scored UNDERPOWERED, not as a Rule 4 failure.**
   This is the third consecutive Rule 4 read to hit its deadline underpowered; the pattern
   is now the strongest single argument for the entry-bar amendment.
2. **Content debt: 19 days since the last MDX post; the ticket is 12 days unanswered.
   The 21-day mark falls on 2026-09-04 — the day after tomorrow — and v4 has no rule for
   what happens there.** The loop cannot write itself an escape; the three options are in
   `docs/seo-log/2026-08-21.md`. If the next two runs also descend, that should be stated
   as a standing failure of the escalation mechanism rather than logged a fourth time as
   routine.
3. **The click rate broke flat for the first time since 08-20**: 1.29 → 2.00/day on the
   trailing seven, with 08-30 the best single-day CTR on record (2.7%). Two more windows
   at ≥1.8/day would make it a trend. Watch it before claiming one.
4. **Two pre-registered guards resolved cleanly today** — Uluwatu expired (closed
   failed-but-stable), the control's diagnostic condition did not fire. Neither needs
   re-reading; both should be dropped from the rolling read set to keep it legible.
5. **Add the page↔query join to `scripts/gsc-snapshot.mjs`.** One extra `safeQuery` with
   `dimensions:['page','query']`. It retires the inference that Rule 1's visibility gate
   and Rule 2's ownership audit have both been running on for 23 runs. **Out of the loop's
   allowed edit scope — this needs the owner or a META run.**
6. **Rule 6 is now exhausted at #1 and #2.** After today, no page on the site lacks a
   BreadcrumbList and no FAQ section lacks FAQPage. What remains under Rule 6: the stale
   `public/sitemap.xml` (1190 bytes, dated Apr 1 — shadowed in production, verified: the
   live `/sitemap.xml` serves the 43-URL dynamic route, so this is dead weight rather than
   a live defect) and the 42-file `og:url` slash mismatch, which needs its own sweep week.
   **Rule 6's budget is now 2/2 for this week and the mechanical sweep is 1/1 — Rule 6 is
   closed until Monday 09-07.**
7. **Duplicate image still unfixed** (carried from 08-26): md5 `f3adcb60abb849b1a03a90ee587f3ac6`
   confirmed identical again today — `rocky-teaching-surf-stance-batu-bolong-canggu.jpg`
   and `surf-instructor-rocky-beginner-lesson-batu-bolong-canggu.jpg`, both in the image
   sitemap. No rule covers it: it is neither a missing alt nor a bad filename.
8. **Sitemap still reports 43 web + 15 image URLs submitted, 0 indexed.** Unchanged all
   month.
9. **First read on the 08-31 canonical sweep: 2026-09-14** — index-form convergence only.
   Today's page report still shows the three slash-less entries (`/surf-lessons-canggu`
   12 impr, `/blog/best-time-to-surf-canggu` 9 impr, `/blog/what-to-expect-first-surf-lesson-bali`
   1 impr), which is the expected state at 2 days, not a result.
