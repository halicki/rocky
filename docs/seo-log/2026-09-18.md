SKIP: DEPENDENCY_INSTALL_FAILED — `npm ci` could not install `googleapis` (via its transitive dependency `zwitch@2.0.4`) after 5 attempts over ~55 minutes, all failing with a deterministic `503 Service Unavailable` from `registry.npmjs.org` (confirmed with direct `curl`, unrelated to the session's egress proxy — `registry.npmjs.org` is in `no_proxy` and connects directly); `node_modules` ended up empty and `scripts/gsc-snapshot.mjs` cannot run without it, so no GSC snapshot was taken and no action was evaluated or implemented today.
