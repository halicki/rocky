# 2026-08-31 — The canonical sweep finally runs after 21 days, and the cannibalisation control gives back a second click

**Day**: 100 (loop-run count)
**Action**: **Rule 6 (Technical Polish — mechanical sweep)** — 42 canonicals moved to the trailing-slash form
**Previous action impact**: **pending, zero exposure days** (the 08-29 rescue shipped after this window closed)

Today is a **Monday**, so both Rule 6 budgets reset (2/week policy, 1/week
mechanical sweep). The canonical mismatch was found and written into PLAYBOOK v3
on **2026-08-10** as *"pierwszy zaplanowany sweep"* and has waited **21 days**
behind higher-priority rules. Rule 4's queue emptied yesterday exactly as its own
notes predicted, so the ladder reached Rule 6 on the first day the sweep was
affordable.

---

## THE HEADLINE: impressions take a 12th consecutive record and the three-window click slide stops

| Metric | 08-29 | **Today** | Δ |
|---|---|---|---|
| Clicks | 41 | **43** | **+2 — ties the ATH set 08-26** |
| Impressions | 4918 | **5036** | **+118 — 12th consecutive all-time high** |
| CTR | 0.83% | **0.85%** | +0.02 |
| Avg position | 13.2 | **13.4** | +0.2 (25th window above 10) |

Window rolled **+2 days** on both ends: 07-30→08-26 becomes **08-01→08-28**.

**The roll explains the gain exactly, and it is stated before the gain is
claimed.** Two days entered — **08-27 (3 clicks, 191 impr)** and **08-28
(2 clicks, 145 impr)**, five clicks between them. Net +2 therefore means the two
departing days (07-30, 07-31) carried **3**. No click was earned on the live site
that would not have been earned anyway.

What is nonetheless true and worth stating: **the three-window decline named in
yesterday's log (43 → 42 → 41) has stopped**, and it stopped by the same
mechanism that produced it. The underlying daily rate has not accelerated —
08-22 (0), 08-23 (1), 08-24 (2), 08-25 (0), 08-26 (2), 08-27 (**3**), 08-28 (2)
is **10 clicks in 7 days, 1.43/day**, against 1.57/day for the preceding seven.
**The impression record is real and the click line is holding, not growing.**
08-27's 3 clicks at 1.6% CTR is the best non-spike day since 08-21.

| | 08-29 | **Today** |
|---|---|---|
| Converting pages | 14 | **15** |
| Converting countries | 12 | **14** (IDN 23, SGP 4, DEU 3, GBR 3, then 1 each × 10) |
| Named visibility | 22% (9 of 41) | **19% (8 of 43)** |
| Device gap | 1.93× | **1.83×** — mobile 28→**28**, desktop 14→**15** |

**The device gap narrowed the good way**: desktop *gained* a click (14 → 15)
rather than mobile losing one. That is the mechanism 08-16 said was missing and
08-20 saw once. Desktop still ranks 19.7 against mobile's 7.7.

**Named visibility fell back to 19%** and the composition is unchanged: **7 of
8 named clicks are Batu Bolong forms** (`batu bolong beach` 2, `batu bolong surf
report` 2, `batu bolong` 1, `surf forecast batu bolong` 1, `surfing batu bolong`
1); the 8th is `surf school near me` (1 impr @ 20.0). **35 of 43 clicks (81%)
still come from queries GSC will not name.**

---

## READS

### Read 1 (registered 08-28, deadline 09-03) — CTR rescue on `/blog/learn-to-surf-cant-swim-bali/`: **PENDING, 1 exposure day**

| | at strike (08-28) | 08-29 | **Today** |
|---|---|---|---|
| Page | 28 impr @ 8.2, 0 cl | 30 @ 8.1, 0 cl | **32 @ 8.0, 0 cl** |

The strike deployed 08-28 and this window ends 08-28, so essentially every
impression above is pre-strike. **Position guard: 8.2 → 8.0, well inside the
±1.5 registered at strike time.** Read stands at 09-03.

### Read 2 (registered 08-29, deadline 09-12) — CTR rescue on `/blog/family-surf-lessons-bali/`: **PENDING, zero exposure days — and the power guard tightens**

| | at strike (08-29) | **Today** |
|---|---|---|
| Page | 26 impr @ 11.1, 0 cl | **22 @ 11.6, 0 cl** |

Position guard (baseline 11.1, a null is VOID if worse than 12.6): **11.6, inside**.

The power guard registered at strike time assumed **0.93 impressions/day**. The
page now draws **22 per 28 days = 0.79/day**, so fourteen exposure days is
~11 impressions and the predicted click count at position ~11 falls from 0.13 to
**~0.11**. The underpowered verdict is *more* certain than when it was written,
not less. Nothing here changes the reading rule: **only a click carries
information; a null on 09-12 reads UNDERPOWERED.**

### Read 3 (registered 08-26, deadline 09-09) — the filename sweep: **pending, expected-null**

`by_search_type` returns **`web` only** for a **fourth** consecutive window;
image impressions remain **zero**. Expected state at 5 days, not a result.

### Read 4 (rolling, 4 exposure days) — the 08-25 homepage internal links

| Target | 08-28 | 08-29 | **Today** |
|---|---|---|---|
| `batu-bolong-beach-canggu-guide` | 1128 @ 11.9, 3 cl | 1159 @ 12.0, 4 cl | **1228 @ 12.3, 4 cl** |
| `surf-camp-vs-surf-lessons-bali` | 10 @ 10.1, 0 cl | 10 @ 7.5, 0 cl | **8 @ 7.4, 0 cl** |
| `surf-lessons-for-women-bali` | 2 @ 12.5 | 2 @ 12.5 | **4 @ 10.3** |
| `do-you-need-to-be-fit-to-surf-bali` | 2 @ 3.0 | 2 @ 3.0 | **2 @ 3.0** |

**`surf-lessons-for-women-bali` moved for the first time in six windows** —
impressions 2 → 4, position 12.5 → 10.3. Flagged specifically because v4
poprawka 8 suspended Priority 2 partly on this page's flatness, and the 08-25 log
recorded that both Priority-2 tests shipped **without inbound link support**,
confounding that evidence. **Four impressions is not a result** and this does not
reopen the 08-28 verdict; it is the first thing that would have to happen if the
confound were real, so it is recorded rather than discarded.

### Read 5 (the 08-21 revert guard on `/blog/canggu-vs-uluwatu-beginners/`): **neither branch fires, for a second consecutive window**

| | at narrowing | 08-28 | 08-29 | **Today** |
|---|---|---|---|---|
| Page | 271 @ 36.9, 2 cl | 279 @ 40.2, 2 cl | 280 @ 40.4, **1 cl** | **278 @ 40.2, 1 cl** |

The 08-29 pre-registration was: *0 clicks with position ≥40 → revert; back to 2 →
closed for good.* It reads **1 click at 40.2**. **The guard does NOT fire, there
is no revert, and the matter does not close.** Position moved 40.4 → 40.2, the
first non-adverse move in four windows.

**The specification defect, named rather than re-argued.** A two-branch guard
with a gap in the middle has now returned the middle value twice and cannot
resolve itself. **Pre-registered here, before the next number is seen:** the
guard exists to catch *decay*, and a click count that is stable at 1 across three
windows with position stable at ~40.2 is not decay. **If the next window reads 1
click with position within ±1.5 of 40.2, the guard EXPIRES and the page is closed
as failed-but-stable.** 0 clicks still fires it; 2 still closes it clean.

### Read 6 (the 08-15 naming strike on `/blog/canggu-surf-forecast/`, closed failed 08-26): **the flattening did not hold**

| | at strike | 08-28 | 08-29 | **Today** |
|---|---|---|---|---|
| `canggu surf report` | 55 @ 22.7 | 49 @ 25.8 | 49 @ 25.8 | **52 @ 26.3** |
| Page | 1017 @ 9.9 | 918 @ 10.7 | 900 @ 10.7, 1 cl | **898 @ 10.6, 1 cl** |

The single flat window reported yesterday was not a floor: the target query drifted
another **0.5 adverse** (25.8 → 26.3) on rising impressions. Verdict unchanged —
**failed**, 22.7 → 26.3 total. The page itself gained a tenth.

### Read 7 (the 08-16 naming strike on `/blog/intermediate-surf-coaching-canggu/`): **the five-window favourable run BREAKS**

| | at strike | 08-21 | 08-26 | 08-28 | 08-29 | **Today** |
|---|---|---|---|---|---|---|
| `intermediate surf lessons` | 3 @ 22.3 | 4 @ 21.3 | 4 @ 21.3 | 4 @ 20.5 | 4 @ 20.5 | **4 @ 20.5** |
| Page | 14 @ 16.3, 0 cl | 20 @ 17.7, 1 cl | 21 @ 16.9, 1 cl | 21 @ 16.8, 1 cl | 22 @ 16.0, 2 cl | **22 @ 19.0, 2 cl** |

Reported prominently because yesterday's log leaned on this page as **"the loop's
only positive instance of v3 Amendment 1"**, and the page line **gave back most of
its gain in one window: 16.0 → 19.0**, a 3.0-position adverse move that undoes
four windows of progress (18.5 → 17.7 → 16.9 → 16.8 → 16.0 → **19.0**). The query
is unmoved at 20.5 and clicks held at 2.

**The limit that cuts against the alarm is stated too**: on a 22-impression page a
3-position swing is comfortably inside noise, and the page is still 2.7 better than
its 08-21 reading. **No verdict is changed on one window.** What changes is
confidence: v3 Amendment 1 stands at **1–1** across two closed reads and the "1"
is not stable.

### Read 8 (rolling, 14th window) — the 08-03 post and its cannibalisation control

| | 08-28 | 08-29 | **Today** |
|---|---|---|---|
| `/blog/batu-bolong-beach-canggu-guide/` | 1128 @ 11.9, 3 cl | 1159 @ 12.0, 4 cl | **1228 @ 12.3, 4 cl** |
| `batu bolong beach` | 389 @ 13.7, 1 cl | 392 @ 13.3, 2 cl | **412 @ 13.7, 2 cl** |
| `batu bolong` | 189 @ 10.0, 1 cl | 195 @ 10.1, 1 cl | **207 @ 10.0, 1 cl** |
| **control** `/batu-bolong-surf/` | 1650 @ 8.7, 15 cl | 1639 @ 8.6, 14 cl | **1669 @ 8.8, 12 cl** |

**The control's clicks fell a second time: 15 → 14 → 12, −20% in two windows,
while its impressions ROSE (1639 → 1669) and its position held (8.6 → 8.8).**

The 08-29 pre-registration was: *"if it drops again next window **while the post
keeps gaining**, that is the first real evidence of harm in 13 windows."*
**The condition is half met and that is stated precisely rather than rounded up:**
the control dropped again, but the post **held at 4 clicks** rather than gaining.

**Roll accounting, which is the honest counterweight.** Three clicks left the
window with 07-30/07-31 and the control lost **2** of them; five clicks entered
with 08-27/08-28 and the control took **0**. So the decline is fully explained by
which days aged out — *and* the control earning nothing on the two best recent
days is itself the observation. Both readings survive this window's data.

**Pre-registered, before the next number:** if the control reads **≤12 clicks
again next window with impressions ≥1600**, that is a third consecutive
non-improving window and it earns a **dedicated diagnostic run** — not an
automatic revert. The 08-03 post is the loop's only causally validated content
win; reverting it on roll-confounded evidence would be a worse error than
waiting one more window.

---

## Action chosen

```
LADDER WALK (every step re-derived from today's window, nothing inherited):

ZASADA NADRZEDNA (content rhythm) — ESCALATED, TICKET UNANSWERED (10 days).
  Verified, not assumed: `git log --since=2026-08-21 -- docs/` returns only
  this loop's own log commits and no file in docs/ carries an owner reply.
  v4: a run finding the ticket unanswered "nie wymysla czwartej opcji i nie
  wraca do pisania postow — schodzi drabina nizej". Done; the run descends.
  Days since last MDX post: 17 (08-14). Conversions in a row: 2/3, unchanged.

RULE 1 — NO MATCH (6th consecutive run). Band 10-20 with >=10 impr:
  | Page                                    | impr @ pos  | Verdict |
  | /blog/batu-bolong-beach-canggu-guide/   | 1228 @ 12.3 | EXCLUDED — live 08-03 experiment + 08-25 link target |
  | /blog/canggu-surf-forecast/             |  898 @ 10.6 | "NAMED AND STILL LOSING" (v4 popr. 9) — read closed failed 08-26, drifted again today |
  | /surf-lesson-prices-bali/               |  278 @ 14.1 | REJECTED by v4 popr. 9 — the rule's own source case |
  | /blog/how-long-to-learn-surfing-bali/   |   41 @ 10.6 | QUERY-ANONYMOUS — no "how long" query in the 137-query report |
  | /blog/family-surf-lessons-bali/         |   22 @ 11.6 | LOCKED — struck 08-29, read open to 09-12 |
  | /blog/intermediate-surf-coaching-canggu/|   22 @ 19.0 | NO ACTION LEFT — naming fully applied; open read, adverse today |
  | /surf-lessons-canggu                    |   17 @ 11.2 | NAMING ALREADY DONE — verified 08-29; only >=3-impr candidate is a plural variant of its own title |
  | /blog/surfing-bali-complete-beginners-guide/ | 12 @ 20.4 | OUT OF BAND (>20) |
  | /blog/how-to-plan-surf-trip-bali/       |   11 @ 30.5 | OUT OF BAND |
  | /blog/solo-surf-lessons-canggu/         |   11 @ 18.5 | QUERY-ANONYMOUS — no "solo" query in the report |
  | /blog/batu-bolong-surf-forecast/        |   10 @ 16.2 | EXCLUDED — control-contaminating (08-03 cluster) |
  ( /blog/when-to-book-surf-lessons-canggu/ 37 @ 9.8 and /blog/private-vs-group-
    surf-lesson-bali/ 21 @ 21.6 have both left the band since 08-29. )

RULE 2 — BLOCKED by the escalation. Ownership audit deliberately NOT re-run:
  it would be the 8th consecutive failure and v4 forbids substituting a
  fourth option while the ticket stands.

RULE 3 — NO MATCH. Every query at >=5 impressions has an owner: Batu Bolong
  place-names ~810, brand-nav msw/surfline ~495, Uluwatu 179 (editorial ban;
  1-B failed 08-28), `canggu surf report` 52, Seminyak ~33 (08-06 post),
  `surfing lessons bali price` 16 (prices page), `group surf lessons` 4 and
  `intermediate surf lessons` 4 (both owned). Largest genuinely un-owned
  intent is still `berawa beach surfing` — below the reporting floor.

RULE 4 — NO MATCH, first time the queue empties by exclusion of every
  candidate rather than by locks alone. See the table below.

RULE 5 — NO MATCH, both branches checked today rather than assumed:
  (a) `by_search_type` returns web only; image impressions ZERO for a 4th
      consecutive window, so the ">=100 image impressions" branch fails;
  (b) every <Image>/<img> in src/ carries alt text — including the two
      multi-line JSX cases in Gallery.tsx and About.tsx, opened and read —
      and the 08-26 sweep left no uninformative filenames.
  The one open image defect (the byte-identical duplicate pair, md5
  f3adcb60abb849b1a03a90ee587f3ac6 confirmed again today) is neither a
  missing alt nor a bad filename and does not trigger this rule.

RULE 6 — MATCH. Today is Monday: weekly budget 0/2, mechanical sweep 0/1.
  (Last week's spend: Rule 6 #3 on 08-25 Tue, sweep on 08-26 Wed.)
```

**The Rule 4 queue, and the near-miss that was checked properly**

| Page | impr @ pos | CTR | Verdict |
|---|---|---|---|
| `/batu-bolong-surf/` | 1669 @ 8.8 | 0.7% | EXCLUDED — the cannibalisation **CONTROL**, 14 live windows |
| `/blog/batu-bolong-beach-canggu-guide/` | 1228 @ 12.3 | 0.3% | EXCLUDED — the 08-03 experiment itself |
| **`/blog/canggu-surf-forecast/`** | **898 @ 10.6** | **0.1%** | **EXCLUDED — see below** |
| `/blog/canggu-vs-uluwatu-beginners/` | 278 @ 40.2 | 0.4% | too deep; on-page work ended 08-28 |
| `/surf-lesson-prices-bali/` | 278 @ 14.1 | 0% | snippet already correct (verified 08-29) |
| `/blog/surf-lessons-seminyak-kuta-legian/` | 133 @ 28.6 | 0% | too deep |
| `/blog/` | 51 @ 41.9 | 0% | index page, too deep |
| `/blog/canggu-surfing-safe-for-kids/` | 50 @ 7.4 | **2.0%** | CTR not < 2%; converting |
| `/blog/how-long-to-learn-surfing-bali/` | 41 @ 10.6 | **2.4%** | CTR not < 2% |
| `/blog/when-to-book-surf-lessons-canggu/` | 37 @ 9.8 | 0% | LOCKED to 09-02 |
| `/blog/learn-to-surf-cant-swim-bali/` | 32 @ 8.0 | 0% | LOCKED — read 09-03 |
| `/beginner-surf-lessons-bali/` | 27 @ 36.0 | 0% | too deep |
| `/blog/family-surf-lessons-bali/` | 22 @ 11.6 | 0% | LOCKED — read 09-12 |
| `/blog/private-vs-group-surf-lesson-bali/` | 21 @ 21.6 | 0% | too deep (page 3) + underpowered |
| `/blog/best-surf-school-bali-how-to-choose/` | 19 @ 43.1 | 0% | < 20 impr AND too deep |

`/blog/canggu-surf-forecast/` was **not waved past**. It is the only page on the
site that satisfies Rule 4's literal condition *and* the power criterion the
08-29 run proposed (≥70 impressions/window — it has 898, ~32/day), which made it
the strongest Rule 4 candidate in weeks, and its description carries two of the
three catalogued defect classes (≈44 of 143 characters restate the title; **no
CTA at all**). It was rejected anyway, on **Rule 4's own re-rescue guard**:

> *"jeśli strona miała CTR rescue i po 14 dniach dalej 0% CTR — sprawdź Google
> Cache (czy nowy title w ogóle się zaindeksował) zanim zrobisz kolejny rescue."*

That check has already been performed and recorded. The **07-31 Rule 4 rescue on
this exact page formally FAILED on 08-10** — six exposure days, CTR 0.2% → 0.1%
against a 0.5% bar — and the 08-15 log states the outcome in the guard's own
terms: **"The title indexed; the CTR did not move."** A third snippet rewrite
would repeat a measured failure on a page that has now had two.

The structural reason is also on the record and was re-derived from today's
window: **~495 of the page's 898 impressions are brand-navigational** (`surfline
batu bolong` 196, `msw canggu` 140, `magicseaweed batu bolong` 96, `batu bolong
surfline` 41, plus smaller forms), ranking **4.7–9.7** and returning **zero
clicks across five windows**. People typing `msw canggu` want magicseaweed.com.
No snippet converts them, and the title cannot be pointed at the addressable
half without moving Batu Bolong language onto a page that does not own it —
which the cannibalisation guard forbids outright while the 08-03 control is live.

```
ACTION: Rule 6 (Technical Polish) — MECHANICAL SWEEP: 42 canonicals to the
        trailing-slash form
TARGET: 41 files under src/app with a literal canonical + src/app/blog/page.tsx
RATIONALE: `next.config.ts` sets `trailingSlash: true`, so the server 308s
        /x -> /x/, and every one of the 43 sitemap <loc> entries already
        carries the slash — but ALL 41 literal canonicals in src/app plus the
        /blog template literal pointed at the slash-LESS form. Google was
        receiving a self-contradictory signal from the same site, and today's
        report shows it resolved inconsistently: 33 of 36 reported pages in
        the slash form, and 3 — /surf-lessons-canggu (17 impr, 1 click),
        /blog/best-time-to-surf-canggu (10 impr @ 7.2) and
        /blog/what-to-expect-first-surf-lesson-bali (1 impr) — reported ONLY
        in the slash-less form, i.e. indexed at URLs that 308-redirect.
        Written into PLAYBOOK v3 on 2026-08-10 as "pierwszy zaplanowany sweep"
        and deferred 21 days behind higher-priority rules; today is the first
        Monday (fresh 0/1 sweep budget) on which Rule 4 is also empty, which
        is precisely the condition the 08-29 notes named for it.
HYPOTHESIS: no ranking or click effect is claimed. The measurable prediction
        is INDEX-FORM CONVERGENCE: by 2026-09-14 the three slash-less entries
        above should disappear from the GSC page report and reappear in the
        slash form, and the site should stop reporting any page in both forms
        at once (the state /blog/surf-coaching-video-analysis-canggu was in
        on 08-10). Sitemap "indexed: 0" is unrelated and is NOT part of this.
RISK, STATED: the three pages currently indexed slash-less are being asked to
        re-index at a different URL, so a short-term wobble on them is
        possible — combined exposure is 28 impressions and 1 click, and they
        are currently indexed at a redirecting URL, which is the worse state.
```

---

## Implementation

**Mechanical sweep — every condition in the Rule 6 v3 exception verified, not asserted:**

| Requirement | Evidence |
|---|---|
| identical pattern in every file (sed-able) | one `perl -pi -e` regex appending `/` inside the canonical string; one second regex for the single template literal |
| does not touch user-visible text or `<title>`/`<meta description>` | `git diff -U0 \| grep -E '^[+-][^+-]' \| grep -vc 'canonical:'` → **0**. Zero changed lines that do not contain `canonical:` |
| verifiable by one grep before and after | before: **41** slash-less literals, **0** with slash. after: **0** slash-less, **41** with slash |
| `npm run build` passes | ✅ passed, all 43 routes prerendered |
| the log names every changed file | below |

**Diff shape**: `42 files changed, 42 insertions(+), 42 deletions(-)` — exactly
one line per file, a pure 1:1 string swap.

**Deliberately left out of scope, and why:**
- **`src/app/layout.tsx`** (`canonical: SITE_URL`) — a **forbidden file**, and the
  root is not a real mismatch: `https://surfingwithrocky.com` and
  `.../` normalise to the same URL.
- **42 slash-less `og:url` values** — `og:url` is an Open Graph field, not a
  Google canonical signal, and including it would make the sweep **two patterns**
  instead of one, breaking the exemption's own precondition. Noted as a
  follow-up, not fixed today.
- **`next.config.ts`** — forbidden file; untouched. The sweep aligns the
  canonicals *to* its behaviour rather than changing it.

**Files changed (42)** — all one canonical line each:

`src/app/blog/page.tsx` (template literal `${SITE_URL}/blog` → `${SITE_URL}/blog/`),
`src/app/batu-bolong-surf/page.tsx`, `src/app/beginner-surf-lessons-bali/page.tsx`,
`src/app/private-surf-lesson-bali/page.tsx`, `src/app/surf-lesson-prices-bali/page.tsx`,
`src/app/surf-lessons-canggu/page.tsx`, and the 36 blog posts under
`src/app/blog/<slug>/page.mdx`:
`bali-surf-itinerary-7-days`, `batu-bolong-beach-canggu-guide`,
`batu-bolong-surf-forecast`, `best-surf-school-bali-how-to-choose`,
`best-surf-spots-canggu-intermediates`, `best-time-to-surf-canggu`,
`canggu-surf-forecast`, `canggu-surfing-safe-for-kids`,
`canggu-vs-uluwatu-beginners`, `do-you-need-to-be-fit-to-surf-bali`,
`family-surf-lessons-bali`, `how-long-to-learn-surfing-bali`,
`how-much-surf-lesson-cost-bali`, `how-to-catch-more-waves-canggu`,
`how-to-plan-surf-trip-bali`, `how-to-pop-up-surfing-bali`,
`how-to-read-a-wave-canggu`, `intermediate-surf-coaching-canggu`,
`is-batu-bolong-good-for-beginners`, `kids-surf-lessons-canggu`,
`learn-to-surf-cant-swim-bali`, `learn-to-surf-over-40-bali`,
`private-vs-group-surf-lesson-bali`, `solo-surf-lessons-canggu`,
`surf-camp-vs-surf-lessons-bali`, `surf-coaching-video-analysis-canggu`,
`surf-etiquette-canggu-lineup`, `surf-lesson-gift-bali`,
`surf-lessons-for-women-bali`, `surf-lessons-near-me-canggu`,
`surf-lessons-seminyak-kuta-legian`, `surfing-bali-complete-beginners-guide`,
`what-to-bring-surf-lesson-bali`, `what-to-expect-first-surf-lesson-bali`,
`what-to-wear-surfing-bali`, `when-to-book-surf-lessons-canggu`.

**Commit**: `e8fd5a0`
**Deployment**: `dpl_4U5JgA7pExBSoKpZmAka4HwmrMZ6` — **READY** (production),
commit SHA `e8fd5a0ff1c2efeb95220c2af255a414ebfbefea`

**Smoke test: PASS** — the three pages that were indexed slash-less were checked
first, then a control and the blog index:

| URL | status | served canonical |
|---|---|---|
| `/surf-lessons-canggu/` | **200** | `.../surf-lessons-canggu/` ✓ |
| `/blog/best-time-to-surf-canggu/` | **200** | `.../blog/best-time-to-surf-canggu/` ✓ |
| `/blog/what-to-expect-first-surf-lesson-bali/` | **200** | `.../blog/what-to-expect-first-surf-lesson-bali/` ✓ |
| `/batu-bolong-surf/` | **200** | `.../batu-bolong-surf/` ✓ |
| `/blog/` | **200** | `.../blog/` ✓ |

Plus the three-way agreement the sweep exists to create, verified live:
- `GET /surf-lessons-canggu` → **308 → `https://surfingwithrocky.com/surf-lessons-canggu/`**
- that URL's canonical → **the same slash form** ✓
- `sitemap.xml` → **43 `<loc>` entries, all in the slash form** ✓
- **Control**: `/surf-lessons-canggu/` `<title>` still
  `Surf Lessons in Canggu, Bali | Surfing With Rocky` — unchanged, confirming the
  sweep is invisible to users and to the snippet ✓

---

## Notes for tomorrow

1. **Content debt: 17 days since the last MDX post; the ticket is 10 days
   unanswered.** **21 days falls on 2026-09-04** and v4 has no rule for what
   happens there. The loop cannot write itself an escape — the three options are
   in `docs/seo-log/2026-08-21.md`.
2. **The control's second click loss is the most important open item.** 15 → 14 →
   12 on rising impressions. The pre-registration above is deliberate: ≤12 again
   next window with impressions ≥1600 buys a **diagnostic run**, not a revert.
3. **`intermediate-surf-coaching-canggu` gave back 3 positions in one window**
   (16.0 → 19.0). One window on a 22-impression page changes no verdict, but v3
   Amendment 1's only positive instance is no longer stable and should be read
   again before being cited.
4. **The Uluwatu guard now has an expiry condition** (Read 5) so it stops
   returning the middle value indefinitely.
5. **Rule 6's remaining named candidate is the stale `public/sitemap.xml`**;
   Rule 6 budget is now **1/2** for this week and the mechanical sweep is
   **1/1, spent**. The `og:url` slash mismatch (42 files) is a *new* named
   candidate created by today's action — it is a second, separate pattern and
   would need its own sweep week.
6. **The power-guard amendment to Rule 4 is still the first item for the next
   META run**, and today strengthens the case: Rule 4's queue is empty at its own
   entry bar, while the one page with enough impressions to be testable is barred
   by its own re-rescue guard.
7. **Duplicate image still unfixed** (carried from 08-26): md5 confirmed
   identical again today — `rocky-teaching-surf-stance-batu-bolong-canggu.jpg`
   and `surf-instructor-rocky-beginner-lesson-batu-bolong-canggu.jpg`, both
   listed in the image sitemap.
8. **Sitemap still reports 43 web + 15 image URLs submitted, 0 indexed.**
   Unchanged all month and explicitly *not* what today's action addresses.
9. **First read on the canonical sweep: 2026-09-14** — index-form convergence
   only, per the hypothesis above. No click or position claim is attached to it.
