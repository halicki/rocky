# 2026-06-28 — HOLD #2 (kids 06-14 title CONFIRMED live-but-not-converting → no 3rd rescue)

**Day**: 63 (loop-run count)
**Action**: **HOLD** — no code change, log-only commit
**Previous action impact**: 06-27 was itself a HOLD (no code change) → nothing to measure. The last *code* action, 06-26 Rule 2 `how-to-plan-surf-trip-bali` (`04c8543`), remains **PENDING** (shipped 06-26; window ends 06-25 → post-dates the window). Sitemap-listed.

## The one new fact today: the kids 06-14 title is live and not converting

The 06-27 log pre-queued today's run as *"first clean lever in days: Rule 4 CTR rescue on `/blog/canggu-surfing-safe-for-kids/`"* with an explicit precondition — **sanity-check the live title (WebFetch) before re-rescuing**. I did, and it changes the disposition:

- **Raw HTML confirms the 06-14 rescue is fully deployed:**
  - `<title>` = `Is Canggu Surfing Safe for Kids? Yes — A Coach's 2026 Guide`
  - meta description = `Yes — from age 6 on Batu Bolong's mellow whitewash. A Canggu coach on safe ages…`
  - (WebFetch's markdown view only exposes the **H1**, still the original "An Honest Family Guide (2026)" — the rescues change the Next.js `metadata` export, not the visible MDX heading. `curl` of the `<head>` is the reliable check.)
- The 06-14 title has therefore been **live and serving for 14 days** and the page is **still 0 clicks / 51 impr / pos 8.0**.

This converts the kids pool from *"can't confirm deployed"* into *"deployed-but-not-converting"*, and that **kills the 3rd-rescue case**, not strengthens it:

1. **PLAYBOOK Rule 4 explicit guard:** *"jeśli strona miała CTR rescue i po 14 dniach dalej 0% CTR — sprawdź Google Cache (czy nowy title się zaindeksował) zanim zrobisz kolejny rescue."* This is exactly the kids situation (rescued 06-14, 14d elapsed, still 0%). The loop has **no Google-cache/URL-inspection tool**, so the precondition for a 3rd rescue is unsatisfiable. Either Google re-crawled the 06-14 title and it simply doesn't convert (title copy is **not** the lever), or it hasn't re-crawled yet (a 3rd rewrite won't help **faster**). Both branches → don't rewrite again.
2. **Marginal pool, not a real anomaly.** At pos 8.0, SERP CTR is ~1.5%, so 51 impr ⇒ **~0.8 expected clicks**. 0 clicks here is roughly what you'd expect at that position/sample — this page is more position/sample-bound than CTR-anomalous. The 06-27 framing ("8.0 = CTR is the genuine lever") slightly over-credited it.
3. **Consistency with 06-27.** We deferred the *larger* price rescue (331 impr — a genuine anomaly: ~4 expected clicks, 0 actual) for can't-confirm-reindex. A **3rd** kids rescue on a **smaller, marginal** pool with the **same** reindex gate is strictly weaker. Deferring price but re-rescuing kids would be incoherent.

This is the page's would-be **3rd** title rewrite (05-19 → 06-14 → today). Two rewrites already produced zero clicks. The honest read: the constraint is crawl/reindex lag + position, not snippet copy.

## Why not a content post (Rule 2 secondary clause)

Mandatory not due (2d since 06-26; fires ~06-29). The only Rule 2 path is the weak secondary clause (Rule 1 ∧ Rule 3 no match), satisfied by any new URL. But:

- **Content supply is not the binding constraint — crawl/index throughput is.** Five recent posts (06-16 kids-lessons, 06-21 family, 06-24 over-40, 06-25 solo, 06-26 trip-planning) are **all still PENDING** (not ranking). Adding a 6th un-indexed orphan today does not move metrics; it lengthens the indexing backlog.
- A post today would be the **4th in 5 days** (06-24/25/26/28) and **worsens spacing** vs. simply shipping tomorrow's mandatory on the clean 3-day rhythm. Same post count, worse cadence, no data signal.
- The 06-26 and 06-27 logs both pre-committed to HOLD over a 4th-in-a-week filler post. 06-28 conditions are identical.

## Pending-action ledger (window 2026-05-29 → 2026-06-25; +1d vs 06-27)

- **06-26 `how-to-plan-surf-trip-bali` (`04c8543`) — PENDING** (post-dates window). First read ~07-03+.
- **06-25 `solo-surf-lessons-canggu` (`741139d`) — PENDING.** Not yet in top pages.
- **06-24 `learn-to-surf-over-40-bali` (`ece8d3e`) — PENDING.**
- **06-23 Rule 6 BreadcrumbList `/surf-lessons-canggu` (`b3e24a6`) — PENDING.** pos 4.0 on 1 impr (noise).
- **06-21 `family-surf-lessons-bali` (`96306f6`) — PENDING.**
- **06-16 `kids-surf-lessons-canggu` (`ee6e641`) — PENDING.** Sibling kids-safety page holds 51 impr / pos 8.0 — demand valid.
- **06-14 Rule 4 kids CTR rescue (`6285be8`) — PENDING, NOT re-rescued today.** Title CONFIRMED live 14d, still 0 cl / 51 impr / pos 8.0. 3rd rescue blocked (reindex-unconfirmable + marginal pool).
- **06-10 Rule 4 price CTR rescue (`b27e762`) — PENDING / deferred.** 324→**331** impr, 0 cl, pos 9.1. Deferred (can't-confirm-reindex; 2×-rescued).
- **06-09 / 06-06 forecast spokes (`0ec506e`, `b948490`) — PENDING.** Forecast slice still on the commercial page: `magicseaweed batu bolong` **140**, `surfline batu bolong` **34**, `batu bolong surf forecast` **29**, `batu bolong surf report` **25**, `surf forecast batu bolong` **22**, `surf report batu bolong` **11**. Pull-off to spokes not yet visible.
- **06-01 Rule 4 `best-time-to-surf-canggu` (`9dc7f67`) — CONFIRMED POSITIVE, holding.** 70 impr / 1 cl / 1.4% / pos 8.0. Left untouched.
- **05-31 Rule 1 `/beginner-surf-lessons-bali` (`3a90d19`) — REGRESSION persists, sub-threshold.** 1 impr / pos 62. No action (sample << 10).

## GSC Snapshot (28-day window: 2026-05-29 → 2026-06-25)

Window rolled forward **+1 day** vs 06-27.

### Totals
- Clicks: **11** (Δ **0** — `/` 8 + `/batu-bolong-surf/` 2 + best-time 1)
- Impressions: **1587** (Δ **+41** — new high; `/batu-bolong-surf/` forecast/brand-nav engine 1010→1041)
- CTR: **0.69%** (Δ **−0.02** — impr outpacing clicks; CTR remains THE bottleneck)
- Avg position: **9.3** (Δ **−0.1** favorable — ~19th consecutive sub-10 day; baseline 22.3 → 9.3)

### Top queries
| Query | Clicks | Impr | CTR | Pos | Δ vs 06-27 |
|---|---|---|---|---|---|
| magicseaweed batu bolong | 0 | **140** | 0% | 5.5 | −2 — largest pool, brand-navigational (structurally low-CTR) |
| surfline batu bolong | 0 | 34 | 0% | 8.1 | +3 (brand-nav) |
| batu bolong surf forecast | 0 | 29 | 0% | 8.7 | +1 — generic, winnable (forecast-spoke target) |
| batu bolong surf report | 0 | 25 | 0% | 8.1 | +1 (generic) |
| surf forecast batu bolong | 0 | 22 | 0% | 9.8 | +1 (generic) |
| batu bolong surf | 0 | 18 | 0% | 25.6 | flat, broad |
| batu bolong beach | 0 | 17 | 0% | 52.5 | flat, broad |
| batu bolong | 0 | 13 | 0% | 49.1 | flat, broad |
| surf report batu bolong | 0 | 11 | 0% | 6.7 | +1 (best-ranked forecast term) |
| magicseaweed canggu batu bolong | 1 | 10 | 10% | 8.5 | flat — **only forecast-cluster converter** |

No kids/family/over-40/solo/trip query at query level (GSC hides sub-3-impr). Page-level kids signal holds (`/blog/canggu-surfing-safe-for-kids/` 51 impr / pos 8.0).

### Top pages
| Page | Clicks | Impr | CTR | Pos | Note |
|---|---|---|---|---|---|
| / | **8** | 95 | 8.4% | 7.7 | **primary click engine** (96→95) |
| /batu-bolong-surf/ | **2** | **1041** | 0.2% | 9.6 | impr engine (1010→1041); absorbing forecast cluster |
| /blog/best-time-to-surf-canggu | 1 | 70 | 1.4% | 8.0 | **06-01 rescue CONFIRMED, holding** (68→70) |
| /surf-lesson-prices-bali/ | 0 | **331** | 0% | 9.1 | 06-10 rescue PENDING/deferred (324→331) |
| /blog/canggu-surfing-safe-for-kids/ | 0 | 51 | 0% | 8.0 | **06-14 rescue PENDING; title CONFIRMED live, not re-rescued** |
| /blog/ | 0 | 12 | 0% | 14.8 | index (lists all posts) |
| /private-surf-lesson-bali/ | 0 | 4 | 0% | 6.5 | 0-click, top-7 |
| /surf-lessons-canggu | 0 | 1 | 0% | 4.0 | 06-23 BreadcrumbList PENDING (1-impr noise) |
| /beginner-surf-lessons-bali | 0 | 1 | 0% | 62 | REGRESSION persists (sub-threshold) |

### By device
- MOBILE: **7** cl / 772 impr / **0.9%** / pos 7.3 — converting
- DESKTOP: **4** cl / 811 impr / **0.5%** / pos 11.3 — biggest low-CTR pool (the CTR drag)
- TABLET: 0 / 4

### By country
- IDN: **6** cl / 725 impr / 0.8% / pos 7.8 — primary market
- NZL: 2 cl / 31 / **6.5%** — small high-CTR converter
- USA: **2** cl / 260 impr / 0.8% / pos 9.5 — biggest foreign pool
- AUS: 1 cl / 63 / 1.6% / pos 14
- NLD 0/81, DEU 0/49, GBR 0/36 — long-tail, 0 clicks

### By search type
- web: 11 cl / 1587 impr / 0.69% / pos 9.3 (only type returned; **no image type → no Rule 5**)

### Sitemap
- `sitemap.xml`: **29 web / 0 indexed + 15 image / 0 indexed**, lastDownloaded **2026-06-27T05:15**, errors 0. Per the 06-27 correction, `indexed: 0` is a **sitemap-attribution lag artifact**, not a site-wide index census (1587 impr proves pages are indexed/serving). De-prioritized as a gate.

## Rule walk-through

- **Mandatory content trigger?** Last MDX 06-26; today = 2 days → **NOT due** (need ≥3d; next ~06-29). Mandatory Rule 2 does NOT fire.
- **Rule 1 (striking distance, rank-bound pos 5–20, ≥10 impr):** every ≥10-impr page is top-10/CTR-bound (`/` 7.7, prices 9.1, best-time 8.0, kids 8.0) or brand-nav forecast on `/batu-bolong-surf/` (9.6, deliberately not strengthened). `/blog/` (14.8) is a list page, not a content target. **No match.**
- **Rule 2 (content):** mandatory not due. Secondary clause technically matches via "new URL is always orthogonal" = content velocity, not a data-driven target — and content supply is not the constraint (5 posts pending-index). Overridden by HOLD failure-mode.
- **Rule 3 (gap, ≥5-impr query w/o page):** all ≥5-impr queries are batu-bolong/forecast (have pages) or brand-nav. **No gap.**
- **Rule 4 (CTR rescue):** kids fails the reindex-before-re-rescue guard (title CONFIRMED live 14d, still 0%, marginal pool); price deferred (can't-confirm-reindex + 2×-rescued, position-bound 9.1); best-time CONFIRMED-positive (leave); `/batu-bolong-surf/` brand-nav (counter-strategic). **No clean target.**
- **Rule 5 (image):** no `image` search type. **N/A.**
- **Rule 6 (technical polish):** budget **2/2 exhausted** this week (06-22 + 06-23; resets Mon 06-29). **Blocked.**
- → **No higher rule matches; the only Rule 2 path is a 4th-in-5-days filler post while 5 posts are pending-index.** Per the failure-mode table ("yesterday's action pending AND no other rule matches a different target → HOLD"), the correct action is **HOLD**.

## Action chosen

```
ACTION: HOLD — no clean lever; kids 06-14 title CONFIRMED live-but-not-converting (no 3rd rescue)
TARGET: — (no code change)
RATIONALE: Non-mandatory day (2d since 06-26). Rule 1/3/5 no match; Rule 6 budget
  2/2 exhausted (resets 06-29). The pre-queued kids Rule 4 rescue is GATED: raw-HTML
  check confirms the 06-14 title has been live & serving 14 days yet the page is still
  0 cl / 51 impr / pos 8.0 — a 3rd rewrite is barred by the PLAYBOOK "confirm reindex
  before re-rescue" guard (no cache tool) and the pool is marginal (pos 8 ⇒ ~0.8
  expected clicks, so 0 isn't an anomaly). Price deferred for the same reindex reason
  on a 2×-rescued page. The only matching rule is Rule 2's weak secondary clause = a
  4th-in-5-days filler while 5 posts await indexing (crawl throughput, not content
  supply, is the constraint).
HYPOTHESIS: no metric change expected from HOLD. 06-29: Rule 6 budget resets (0/2) AND
  mandatory content window opens (3d since 06-26) → ship the clean queued topic
  (intermediate-surf-coaching-canggu / bali-surf-itinerary-7-days / where-to-stay-
  canggu-surfers; avoid forecast + demographic overlap). The 5 PENDING posts + 06-23
  breadcrumb need crawl time, not more publishing.
```

**Scope discipline:** No code edits. Pre-existing untracked/modified `package.json`/`package-lock.json`/`src/app/icon.svg` left untouched (not mine, as 06-27). Log-only commit.

## Implementation

No code change. Log-only commit (HOLD), per the 06-18 / 06-27 precedent.

## Notes for tomorrow / next run

- **06-29 = the unblocking day.** Rule 6 budget resets (0/2) **and** the mandatory content window opens. Default action: ship the mandatory post on a clean, non-overlapping topic. Queued candidates (from 06-26/06-27): `intermediate-surf-coaching-canggu`, `bali-surf-itinerary-7-days`, `where-to-stay-canggu-surfers`. **Avoid** forecast (still indexing) and beginner/demographic overlap (already 5-deep).
- **Stop re-rescuing the kids page.** Two title rewrites (05-19, 06-14) produced zero clicks; the 06-14 title is confirmed live. A 3rd rewrite is the wrong tool — the issue is crawl/reindex lag + position (pos 8, marginal sample), not snippet copy. Revisit only if the page climbs to top-5 or impressions scale past ~150 with still-zero clicks.
- **Price rescue stays deferred** until the 06-10 title can be confirmed re-crawled (needs a URL-inspection capability the loop lacks). It's the bigger anomaly (331 impr / 0 cl) but also 2×-rescued and position-bound at 9.1.
- **CTR is still THE bottleneck** (0.69%). The 1500+ impr base is non-converting forecast/brand-nav on `/batu-bolong-surf/` (1041 impr at 0.2%). The structural fix is the forecast spokes (06-06/06-09) pulling that traffic off — an indexing/ranking wait, which reinforces today's HOLD over yet another snippet tweak.
- **Owner-escalation watch (~07-01):** if the forecast spokes still show no ranking for generic forecast queries (`batu bolong surf forecast/report` still entirely on `/batu-bolong-surf/`) by ~07-01, flag to owner as a **spoke-crawl/ranking** question (not "site dark" — disproven by 1587 impr).
- **Mild positives:** impr new high **1587**, `/batu-bolong-surf/` 1010→1041, best-time CONFIRMED holding (70 impr/1 cl), pos 9.4→**9.3** (~19th sub-10 day).
- **Tooling gap worth flagging:** two HOLDs in a row are partly a *tooling* artifact — the loop cannot confirm whether a rewritten title has been re-crawled, so every 2nd/3rd CTR rescue is structurally un-actionable. A GSC URL-Inspection API call (available to the same service account) would unblock the price/kids decisions. Owner-deferred enhancement.
