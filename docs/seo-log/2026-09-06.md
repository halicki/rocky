# 2026-09-06 — the rise streak breaks without breaking the trend, and the ladder empties a fourth time

**Day**: 105 (loop-run count)
**Action**: **HOLD — no rule matches.** Log-only run, no code commit. Fourth consecutive.
**Previous action impact**: **not applicable** — 09-05 was a HOLD; nothing shipped.

Window rolled **+1 day** on both ends: 08-06→09-02 becomes **08-07→09-03**.

---

## THE HEADLINE: the six-window rise ends, and the trend it produced survives it

| Metric | 09-05 | **Today** | Δ |
|---|---|---|---|
| Clicks | 48 | **48** | **flat** |
| Impressions | 5123 | **5086** | **−37** |
| CTR | 0.94% | **0.94%** | flat |
| Avg position | 13.6 | **13.7** | +0.1 unfavourable (30th window above 10) |

**Roll accounting, derived not assumed.** 09-03 entered with **1 click / 152 impressions**
(read from `by_date`). The departing day follows by arithmetic: 08-06 = 5123 + 152 − 5086 =
**189 impressions**, and 48 + 1 − 48 = **1 click**. Both deltas are fully explained by the
swap. **Nothing was lost today.**

### The trailing-7 click rate falls for the first time in seven readings — and clears its bar anyway

| Trailing 7 days | Clicks | Rate |
|---|---|---|
| 08-17 → 08-23 | 9 | 1.29/day |
| 08-22 → 08-28 | 10 | 1.43/day |
| 08-24 → 08-30 | 14 | 2.00/day |
| 08-25 → 08-31 | 17 | 2.43/day |
| 08-26 → 09-01 | 19 | 2.71/day |
| 08-27 → 09-02 | 20 | 2.86/day |
| **08-28 → 09-03** | **18** | **2.57/day — first fall** |

The mechanism is the same one-for-one swap as the totals: **08-27 left with 3 clicks and 09-03
arrived with 1.** No day changed.

**The trend called on 09-04 is not disturbed, and this has to be said on the run where the
number goes down rather than only on the runs where it goes up.** The 09-02 log wrote the
condition as *"two more windows of ≥1.8/day"*. Today's 2.57/day clears that bar by **43%**.
What ends is the *monotone rise*, which the loop never pre-registered as anything. A trend
called on a level bar is not falsified by a lower reading above the bar — the same discipline
that entitled the loop to call it on 09-04 forbids re-scoring it today.

### The window's internal split, one day further along

| | Impressions | Clicks | CTR |
|---|---|---|---|
| First 7 days (08-07 → 08-13) | **1437** | 7 | **0.49%** |
| Last 7 days (08-28 → 09-03) | **1073** | **18** | **1.68%** |

Daily impressions **−25%** across the window while daily clicks rose **157%**. Yesterday's
diagnostic located 69% of that impression fall in one page narrowing (`batu-bolong-beach-canggu-guide`,
−210 impressions between the halves). Today's −37 is roll, not decline; the composition story
is unchanged and is not re-derived.

| | 09-05 | **Today** |
|---|---|---|
| Converting pages | 17 | **16** (−1) |
| Converting countries | 16 | **16** (IDN 20, SGP 5, GBR 4, AUS 3, then DEU/IND/NLD/PHL 2, AUT/CHE/ESP/HKG/JPN/MYS/THA/USA 1) |
| Named visibility | 12.5% (6 of 49) | **12.5% (6 of 48)** — ties the worst |
| Device split | mobile 25 / desktop 23 (1.09×) | **mobile 26 / desktop 22 (1.18×)** |

**Desktop's four-window narrowing streak also breaks** — 1.81× → 1.45× → 1.23× → 1.09× →
**1.18×** — on a two-click swing, which is noise. The position gap is untouched for a
fifth window: mobile **7.8**, desktop **19.8**.

---

## READS

Today is an **off-date** for the only read with a scoring window open. Nothing below is scored
that was not registered to be scored today.

### Read 1 — `/blog/learn-to-surf-cant-swim-bali/` (08-28 CTR rescue): **CLOSED POSITIVE 09-05, recorded only**

| | at strike | 09-04 | 09-05 | **Today** |
|---|---|---|---|---|
| Page | 28 impr @ 8.2, 0 cl | 32 @ 7.3, 0 cl | 29 @ 6.5, 1 cl | **26 @ 6.4, 1 cl, CTR 3.8%** |

Position **6.4**, now marginally better than the guard band's floor (6.7–9.7). The read is
closed; today's row is a record, not a re-score. Its confound stands as written on 09-05 — the
click arrived at position 4.0 against a 6.5 average — and is **not** retro-strengthened by the
page holding position today.

### Read 2 — `/blog/family-surf-lessons-bali/` (08-29 CTR rescue): pending, 5th flat window

| | at strike | 09-03 | 09-04 | 09-05 | **Today** |
|---|---|---|---|---|---|
| Page | 26 impr @ 11.1, 0 cl | 22 @ 12.0 | 22 @ 12.0 | 22 @ 12.0 | **24 @ 12.2, 0 cl** |

Position guard (VOID if worse than 12.6): **12.2, inside.** Real deadline **09-15**.

### Read 3 — the 08-26 filename sweep (deadline 09-09): pending, expected-null

Image impressions **zero for a 9th consecutive window** — re-queried directly today at both
the aggregate and the daily level (`type: image`, 28 daily rows, **all zero**), not inherited
from yesterday. Sitemap still reports **15 image URLs submitted, 0 indexed**.

### Read 4 — the 08-25 homepage internal links (9 exposure days)

| Target | 09-04 | 09-05 | **Today** |
|---|---|---|---|
| `batu-bolong-beach-canggu-guide` | 1325 @ 12.5, 6 cl | 1272 @ 12.7, 4 cl | **1234 @ 12.8, 5 cl** |
| `surf-camp-vs-surf-lessons-bali` | 4 @ 9.8, 0 cl | 4 @ 9.8, 0 cl | **5 @ 8.8, 0 cl** |
| `surf-lessons-for-women-bali` | 4 @ 10.3 | 5 @ 10.4 | **6 @ 11.5** |
| `do-you-need-to-be-fit-to-surf-bali` | 3 @ 5.0 | 3 @ 5.0 | **3 @ 5.0** |

Three targets moved by **one impression each** in nine days. **Still no readable signal.**

### Read 6 — the 08-15 naming strike on `/blog/canggu-surf-forecast/` (closed FAILED 08-26)

| | at strike | 09-04 | 09-05 | **Today** |
|---|---|---|---|---|
| `canggu surf report` | 55 @ 22.7 | 52 @ 26.7 | 52 @ 26.6 | **55 @ 26.5** |
| Page | 1017 @ 9.9 | 958 @ 10.6, 1 cl | 983 @ 10.5, 1 cl | **1008 @ 10.5, 1 cl** |

Impressions returned to the strike-day figure at **3.8 positions worse**. Verdict unchanged.
Today's join re-measures the page at **69% brand-navigational** (msw canggu 165 + magicseaweed
batu bolong 18 + surfline canggu 9 + canggu surfline 5 = 197 of 280 join impressions).

### Read 7 — the 08-16 naming strike on `/blog/intermediate-surf-coaching-canggu/`

| | at strike | 09-04 | 09-05 | **Today** |
|---|---|---|---|---|
| `intermediate surf lessons` | 3 @ 22.3 | 5 @ 20.2 | 4 @ 19.0 | **4 @ 19.0, 0 cl** |
| Page | 14 @ 16.3, 0 cl | 26 @ 20.0, 3 cl | 25 @ 19.8, 3 cl | **23 @ 19.0, 3 cl** |

Flat on 4 impressions. v3 Amendment 1 remains **1–1**.

### Read 8 — the 08-03 post and its control: **TODAY IS NOT A SCORING DATE**

| | 09-04 | **09-05 (reading 1)** | **Today (09-06, off-date)** |
|---|---|---|---|
| `/blog/batu-bolong-beach-canggu-guide/` | 1325 @ 12.5, 6 cl | 1272 @ 12.7, 4 cl | **1234 @ 12.8, 5 cl** |
| **control** `/batu-bolong-surf/` | 1623 @ 9.0, 10 cl | **1621 @ 9.1, 11 cl** | **1586 @ 9.1, 11 cl** |
| `batu bolong beach` | 433 @ 13.7, 2 cl | 414 @ 14.7, 1 cl | **394 @ 15.3, 1 cl** |
| `batu bolong` | 209 @ 10.2, 1 cl | 202 @ 10.3, 1 cl | **191 @ 10.4, 1 cl** |

The registration names **09-05 / 09-07 / 09-09** and scores the **mean of three**. Today is
none of them. The row above is recorded for continuity and **is not a reading** — the same
refusal the 09-04 run made when the off-date number happened to look decisive. Reading 2 is
**tomorrow**; the control's impressions have drifted to 1586, below the registration's ≥1600
condition, and **that is a fact for 09-07 to weigh, not for today to pre-empt.**

Separation re-confirmed in today's 151-row join: on `batu bolong beach` the post sits at
**12.9** and the control at **70.4**.

---

## Action chosen

```
LADDER WALK (re-derived from today's window; the visibility gate and the ownership
test are checked against a fresh 151-row page-query join, fifth consecutive run).

ZASADA NADRZEDNA (content rhythm) — ESCALATED, TICKET UNANSWERED (day 16).
  Verified, not assumed: `git log --since=2026-08-19 -- docs/` returns ten
  commits, all this loop's own `seo(log):` entries. No owner reply exists.
  Last new MDX post, by `git log --diff-filter=A`: 2026-08-14 => 23 DAYS.
  Conversions in a row: 2/3, unchanged. v4 directs a run that finds the ticket
  unanswered to descend the ladder and say so in one sentence. Done — fourth
  such descent.

RULE 1 — NO MATCH (11th consecutive run). Band 10-20 with >=10 impr:
  | Page                                        | impr @ pos  | Named query >=3 impr (JOIN)         | Verdict |
  | /blog/batu-bolong-beach-canggu-guide/       | 1234 @ 12.8 | yes — batu bolong beach 378         | EXCLUDED — live 08-03 experiment, scoring window OPEN |
  | /blog/canggu-surf-forecast/                 | 1008 @ 10.5 | yes — msw canggu 165                | "NAMED AND STILL LOSING" — closed failed 08-26; 69% brand-nav |
  | /surf-lesson-prices-bali/                   |  293 @ 14.9 | yes — surfing lessons bali price 16 | REJECTED by v4 popr. 9 — the rule's own source case |
  | / (homepage)                                |  137 @ 11.4 | YES — batu bolong surf lessons 10   | EXCLUDED on ORTHOGONALITY (see note below) |
  | /blog/how-long-to-learn-surfing-bali/       |   41 @ 10.5 | NO — zero join rows                 | QUERY-ANONYMOUS (5th confirmation) |
  | /blog/family-surf-lessons-bali/             |   24 @ 12.2 | marginal (family surfing bali 2)    | LOCKED — struck 08-29, read open to 09-15 |
  | /blog/intermediate-surf-coaching-canggu/    |   23 @ 19.0 | yes — intermediate surf lessons 4   | NO ACTION LEFT — naming applied 08-16; open read |
  | /blog/surfing-bali-complete-beginners-guide/|   12 @ 19.7 | NO — zero join rows                 | QUERY-ANONYMOUS |
  ONE PAGE LEFT THE BAND UPWARD ON POSITION: /blog/private-vs-group-surf-lesson-bali
  20 impr @ 18.8 -> 21.4, now outside 10-20. No new entrants.

RULE 2 — BLOCKED by the escalation. The ownership audit is deliberately NOT re-run;
  an 11th consecutive manufactured failure would be evidence of nothing.

RULE 3 — NO MATCH, verified at page level for a 5th run. All 33 queries at >=5
  impressions resolve to an owning page in the 151-row join (table computed, not
  eyeballed). The single marginal case is `magicseaweed berawa` (5 impr @ 9.4,
  landing on /blog/ at 9.0) — a COMPETITOR BRAND term already ranking on page 1,
  struck from the fallback list on 08-20 and barred by Rule 2's cannibalisation
  guard from appearing in any new title/H1/H2. Largest genuinely un-owned intent
  remains `berawa beach surfing`, 1 impression @ 50.0.

RULE 4 — NO CANDIDATE. Pages with >=20 impr and CTR <2%, each assigned away by
  Krok 0 or by orthogonality:
    /batu-bolong-surf/         1586 @ 9.1, 0.7%  — IN the validated 5-10 band, and the
                                                   CANNIBALISATION CONTROL with 2 of 3
                                                   scoring dates still to come. Editing it
                                                   would contaminate the loop's only
                                                   causal experiment two days from its end.
    /blog/batu-bolong-beach-canggu-guide/ 1234 @ 12.8 — the experiment's treatment arm
    /blog/canggu-surf-forecast/ 1008 @ 10.5, 0.1%  — rank-bound; naming closed failed
    /surf-lesson-prices-bali/    293 @ 14.9, 0%    — snippet verified correct 08-29
    /blog/canggu-vs-uluwatu-beginners/ 276 @ 39.3  — rank-bound
    /blog/surf-lessons-seminyak-kuta-legian/ 156 @ 27.1 — rank-bound
    /blog/                        51 @ 47.3        — rank-bound
    /blog/when-to-book-surf-lessons-canggu/ 35 @ 7.7, 0% — DECLINED, 5th time (below)
    /beginner-surf-lessons-bali/  29 @ 31.7        — rank-bound
    /blog/family-surf-lessons-bali/ 24 @ 12.2      — locked, read open
    /blog/private-vs-group-surf-lesson-bali/ 20 @ 21.4 — rank-bound

RULE 5 — NO MATCH, both branches re-verified in the files today, not inherited:
  (a) image impressions ZERO for a 9th window, re-queried at daily granularity;
  (b) `grep -rn "<Image|<img" src/` returns exactly 2 sites (Gallery.tsx:28,
      About.tsx:10); both opened, both carry alt. All 10 files in public/images/
      are descriptively named.
  NOTED, NOT ACTIONED (12th day carried): the byte-identical duplicate pair
  (rocky-teaching-surf-stance-batu-bolong-canggu.jpg and
  surf-instructor-rocky-beginner-lesson-batu-bolong-canggu.jpg, both md5
  f3adcb60abb849b1a03a90ee587f3ac6). Rule 5 covers neither duplicates nor their
  fix, and the fix is a deletion.

RULE 6 — CLOSED. Weekly budget 2/2 SPENT (Mon 08-31 canonical sweep e8fd5a0,
  Wed 09-02 BreadcrumbList f92f5fa); mechanical sweep 1/1 SPENT. Verified against
  INDEX.md and `git log`, not carried. Reopens TOMORROW, Monday 09-07.

RULE 7 — CLOSED. Budget 1/week, spent Thursday 09-03. Today is SUNDAY. Resets
  tomorrow with standing failure 2 still unaddressed.

=> NO RULE MATCHES. HOLD.
```

### The homepage exclusion is re-grounded, because the reason given for three runs was wrong

The last three logs excluded `/` from Rule 1 as *"no editable surface"*. That is **inaccurate
and is corrected here rather than repeated**: `src/app/page.tsx` is explicitly inside the
command's allowed scope, and it carries the homepage `<title>`, `<meta description>` and
schema — three of the five levers Rule 1's default action lists. The homepage is editable.

It is excluded for two better reasons:

1. **Orthogonality.** The 08-25 action (`f5c5e33`) added the three contextual-orphan links to
   the homepage. That read is **open** and has no signal after nine exposure days. Rule 1's
   third entry gate bars a page with an open read.
2. **Risk asymmetry.** The homepage converts at **8.8% (12 of 48 clicks — 25% of all site
   clicks)** on 137 impressions. Rewriting its title to chase `batu bolong surf lessons`
   (10 impressions @ 9.4) risks the loop's single largest click source for a query worth
   ~7% of its exposure. Rule 1 has no provision for that trade, and inventing one on a run
   that wants an action is precisely the failure mode v3 was written against.

### `when-to-book` is declined a fifth time — on the one ground yesterday left standing

Yesterday's log falsified ground (a) (query-anonymity) and stated that a run wanting to strike
this page must argue against ground (b). **This run tried and could not.**

| | |
|---|---|
| Today | 35 impr @ 7.7, **0 clicks**, 0% CTR — the only page in the validated 5–10 band with ≥20 impressions and zero CTR |
| Formal Rule 4 fit | **PASSES** — ≥20 impr, CTR <2%, last rescue 08-19 = **18 days** ago (>14) |
| Rule 4's own repeat procedure | **COMPLETED** — the note says check the title indexed before a second rescue; it is indexed (smoke-tested 08-20) |
| Ground (b) | The 08-19 rescue (`f72f209`) **already rewrote both levers**: title → *"When to Book Surf Lessons in Canggu: 6:30 AM, 5 Days Ahead"* (58 ch), description → 145 ch carrying months, slot, lead time and a WhatsApp CTA. Eighteen days later: **0 clicks.** |

**The bar this run set for itself and failed to clear: name a defect in the current snippet
that the second rewrite would fix.** Reading it against Rule 4's own spec — keyword-led title
under 60 characters, description 140–155 characters carrying query + numbers + CTA — the
snippet is *already compliant on every point*. A second rewrite would be churn dressed as an
action, and Rule 4's third lever (`aggregateRating`) is unavailable: this is an editorial
article, and self-serving review markup on it would be schema spam, not a rich result.

Power, restated: 35 impr / 28 days = **1.25/day**. At a good 3% CTR the page earns
**~1 click per month**. The read would be underpowered whichever way it landed — the exact
finding the 08-29 log recorded as *"Rule 4's entry bar cannot support its own read bar."*

**Read 1's positive does not rescue this.** `cant-swim` was struck **once**; `when-to-book` has
been struck once already and failed. The evidence that a first snippet fix can work on a
query-anonymous page in this band is not evidence that a **second** one can.

```
ACTION: HOLD — no rule matches
TARGET: none (log-only run, no file under src/ touched)
RATIONALE: Rule 1 no match for an 11th run, one page leaving the band upward and no
        new entrants. Rule 2 blocked by an escalation unanswered for 16 days. Rule 3
        verified empty at page level against a fresh 151-row join, computed rather
        than eyeballed. Rule 4's only formally-qualifying candidate is declined on
        the single ground yesterday left standing, after this run explicitly tried
        and failed to argue against it. Rule 5 clean on both branches, re-queried
        and re-grepped today. Rules 6 and 7 are budget-closed until tomorrow. The
        command's failure-mode table maps this state to HOLD.
SUBSTANCE DELIVERED INSTEAD: the trailing-7 fall reported on the run where the
        number goes down, with the pre-registered bar re-checked rather than
        re-scored; the homepage exclusion corrected from a wrong reason to two
        right ones; and the `when-to-book` decline tested to destruction instead
        of restated.
HYPOTHESIS: none — no change was made, so no metric is predicted to move.
```

---

## 🎫 STANDING FAILURE 1 — the content escalation, day 16

| | |
|---|---|
| Ticket written | 2026-08-21, `docs/seo-log/2026-08-21.md` |
| Days unanswered | **16** |
| Days since last MDX post | **23** |

Options unchanged: **(a)** keyword research from outside GSC, **(b)** lift the editorial ban on
the Uluwatu pool — today **190 impressions across 7 variants at positions 38.3–81.0, zero clicks
in 19 windows** — or **(c)** approve a post without data support.

## 🎫 STANDING FAILURE 2 — Rule 7's branches compete for one weekly budget

Unchanged. The rule missed its own designated weekday (Friday 09-04) because Thursday's fallback
branch had consumed the week's slot. Budget resets tomorrow; **spending it before Friday would
reproduce the failure**, and the next run should read this line before it does.

## The ladder-emptying: fourth run, and deliberately still not a third ticket

Four consecutive runs have now walked the full ladder and found nothing. Yesterday recorded this
as an observation and declined to escalate it, reasoning that it is the **same defect** the
09-04 log already put to the owner — that Rules 1, 3 and 4 all select targets by named query,
which is structurally blind to this site's best-converting pages — and that multiplying tickets
for one cause makes the queue less useful.

**That reasoning is adopted, not overridden.** What today adds is the fourth data point and its
sharpest illustration: `/blog/kids-surf-lessons-canggu/` is the site's **best-converting page**
(34 impressions, 5 clicks, **14.7% CTR** at position 9.9) and **no rule on the ladder can reach
it** — Rule 1 needs position ≥10 (it is 9.9), Rule 4 needs CTR <2% (it is 14.7%), and it has
**zero join rows**, so every named-query gate excludes it too. The loop's rule set has no
vocabulary for "this page works; do more of it."

---

## Implementation

**No files under `src/` were changed. This is a log-only run.**

`npm run build` was run rather than skipped — **passed, exit code 0**. Working tree otherwise
clean at `284c6fa`.

Steps 7–9 of the loop (code commit / deploy verification / production smoke test) are **not
applicable** and are recorded as skipped-with-reason rather than silently omitted: there is no
code change to deploy, so production is unchanged from `f92f5fa` (09-02) and that run's smoke
test remains the current state of the live site.

**Commits**: 1 (log only). Guardrail is max 1 code + 1 log; 0 + 1 is inside it.

---

## Notes for tomorrow

1. **Read 8's second scoring date is TOMORROW, 09-07**; the third is 09-09. Reading 1 (09-05)
   was control **11 cl / 1621 impr / pos 9.1**. Today's off-date row shows the control at
   **1586 impressions — below the registration's ≥1600 condition.** Score the **mean of three**
   as written; do not treat a single sub-1600 window as the diagnostic trigger, and remember
   the 09-05 mechanism finding: the control's 28-day click count falls while its *daily* rate
   rises, because high-click days are rolling off the back.
2. **Rule 6 reopens tomorrow** with budget 2/2 and mechanical sweep 1/1 restored. Queued and
   **re-measured today**: the `og:url` slash mismatch is **42 occurrences across 41 files**
   (yesterday's "42 files" counted occurrences) — a genuine mechanical sweep matching the
   08-31 canonical pattern, which is itself **verified complete: zero non-slash canonicals
   remain in `src/app`**. Also queued: the stale `public/sitemap.xml` (1190 bytes, dated
   Apr 1, shadowed in production by the generated route).
3. **Rule 7's budget also resets tomorrow — read standing failure 2 before spending it.**
4. **The trend survives its first down-reading.** 2.57/day against a 1.8/day bar. Do not
   re-call it, and do not treat the next fall as a reversal unless it goes below the bar.
5. **`when-to-book` is now declined five times, and this run exhausted the argument** rather
   than deferring it. Do not re-open it on Rule 4 without either new impressions (>60 in a
   window, which would make the read powered) or a named defect in the current snippet.
6. **The homepage is editable** — three logs said otherwise. Its real exclusions are the open
   08-25 read and the 25%-of-all-clicks risk. Re-check the first after 09-08 (14 days).
7. **First read on the 08-31 canonical sweep: 2026-09-14.** Today's page report still shows
   three slash-less entries (`/surf-lessons-canggu` 9 impr, `/blog/best-time-to-surf-canggu` 2,
   `/blog/what-to-expect-first-surf-lesson-bali` 1), down from 9 / 5 / 1 yesterday. Six days
   in — directional only, not a result.
8. **Priority 2's suspension review is 9 days overdue** and its verdict is unchanged: both
   posts null (`do-you-need-to-be-fit-to-surf-bali` 3 impr @ 5.0; `surf-lessons-for-women-bali`
   6 @ 11.5). Moot while Rule 2 is escalation-blocked, but the playbook text is stale.
9. **Add the page↔query join to `scripts/gsc-snapshot.mjs`** — carried for a fifth run. It
   produced today's Rule 3 table, the 69% brand-nav re-measurement and the kids-page
   illustration. **Outside the loop's edit scope — needs the owner or a META run.**
