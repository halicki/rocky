# 2026-09-16 — Rule 5 fires for the first time: image impressions cross the ≥100 bar after three doubling windows

**Day**: 115 (loop-run count)
**Action**: **Rule 5 — Image SEO**. Homepage `ImageGallery` structured data gains a 7th
`ImageObject` entry for the `<About/>` section photo, which was rendered (with `priority`,
above the fold) but absent from both the visible carousel array's schema mapping and any
structured data on the site.
**Previous action impact**: `PENDING (normal 2–7 day crawl delay)`. 09-15's target,
`/surf-lesson-prices-bali/`, moved 12.9 → **12.7** this window — directionally favourable,
but 09-15 explicitly registered no ranking/click claim for a mechanical internal-linking
action, so this is not scored. 09-14's target, `/beginner-surf-lessons-bali/`, held flat at
33.0 for a second window — same non-claim, noted only.

Window rolled **+1 day** on both ends vs 09-15: 08-16→09-12 becomes **08-17→09-13** (not
byte-identical — a fresh firing, not a duplicate).

---

## GSC Snapshot (28-day window, 2026-08-17 → 2026-09-13)

### Totals
- Clicks: **54** (Δ −1 from 55)
- Impressions: **4754** (Δ −24 from 4778)
- CTR: **1.14%** (Δ −0.01 from 1.15%)
- Avg position: **14.0** (flat)

### Top pages (≥10 impressions)

| Page | Clicks | Impr | CTR | Pos | Δ Pos vs 09-15 |
|---|---|---|---|---|---|
| `/` | 17 | 147 | 11.6% | 10.7 | −0.3 (improving) |
| `/batu-bolong-surf/` | 12 | 1450 | 0.8% | 10.3 | +0.1 |
| `/blog/canggu-surf-forecast/` | 0 | 1087 | 0% | 10.4 | flat |
| `/blog/batu-bolong-beach-canggu-guide/` | 6 | 950 | 0.6% | 13.6 | +0.3 (worse) |
| `/surf-lesson-prices-bali/` | 0 | 291 | 0% | 12.7 | −0.2 (improving) |
| `/blog/canggu-vs-uluwatu-beginners/` | 0 | 242 | 0% | 36.3 | −0.1 |
| `/blog/surf-lessons-seminyak-kuta-legian/` | 0 | 137 | 0% | 27.9 | +0.4 (worse) |
| `/blog/best-time-to-surf-canggu/` | 1 | 71 | 1.4% | 49.8 | −1.2 (improving) |
| `/blog/` | 0 | 53 | 0% | 37.7 | −0.1 |
| `/blog/when-to-book-surf-lessons-canggu/` | 0 | 44 | 0% | 7.2 | +0.2 |
| `/blog/how-long-to-learn-surfing-bali/` | 2 | 40 | 5.0% | 9.4 | −0.1 |
| `/blog/canggu-surfing-safe-for-kids/` | 0 | 37 | 0% | 9.2 | +0.2 |
| `/blog/learn-to-surf-cant-swim-bali/` | 3 | 35 | 8.6% | 6.7 | flat |
| `/beginner-surf-lessons-bali/` | 0 | 28 | 0% | 33.0 | flat |
| `/blog/intermediate-surf-coaching-canggu/` | 3 | 24 | 12.5% | 13.7 | −3.2 (improving) |
| `/blog/learn-to-surf-over-40-bali/` | 1 | 22 | 4.5% | 5.2 | flat |
| `/blog/family-surf-lessons-bali/` | 0 | 23 | 0% | 8.4 | −0.1 |
| `/blog/kids-surf-lessons-canggu/` | 4 | 18 | 22.2% | 11.8 | +0.2 (worse) |
| `/blog/solo-surf-lessons-canggu/` | 1 | 19 | 5.3% | 31.9 | +4.8 (worse) |
| `/blog/private-vs-group-surf-lesson-bali/` | 0 | 18 | 0% | **18.7 ← still in the Rule 1 band** | −0.5 |
| `/blog/what-to-bring-surf-lesson-bali/` | 2 | 15 | 13.3% | 5.8 | flat |
| `/blog/best-surf-school-bali-how-to-choose/` | 0 | 14 | 0% | 33.4 | flat |
| `/blog/batu-bolong-surf-forecast/` | 0 | 12 | 0% | 12.8 | −0.3 |
| `/private-surf-lesson-bali/` | 0 | 10 | 0% | 4.3 | flat |

### Top queries (≥20 impressions)

| Query | Clicks | Impr | Pos |
|---|---|---|---|
| batu bolong beach | 1 | 375 | 18.5 |
| msw canggu | 0 | 159 | 9.6 |
| surfline batu bolong | 0 | 130 | 7.3 |
| batu bolong | 1 | 102 | 12.8 |
| magicseaweed batu bolong | 0 | 92 | 4.4 |
| batu bolong surf report | 0 | 67 | 6.1 |
| uluwatu surf beginner | 0 | 61 | 40.6 |
| canggu surf report | 0 | 57 | 26.4 |
| batu bolong surf forecast | 0 | 55 | 7.0 |
| surfing in uluwatu for beginners | 0 | 51 | 42.2 |
| uluwatu surfing beginners | 0 | 48 | 38.4 |
| surf forecast batu bolong | 1 | 35 | 9.7 |
| batu bolong canggu | 0 | 32 | 17.8 |
| batu bolong beach bali | 0 | 29 | 12.1 |
| batu bolong beach canggu | 0 | 28 | 20.5 |
| surfline canggu | 0 | 24 | 9.6 |
| batu balong beach | 0 | 20 | 12.0 |

### `by_search_type`

| type | clicks | impr | pos |
|---|---|---|---|
| web | 54 | 4754 | 14.0 |
| **image** | 0 | **102** | 66.9 |
| news | 0 | 2 | 7.0 |

**RULE 5 FIRES.** Image impressions completed a fourth consecutive doubling window —
5 (09-13) → 29 (09-14) → 62 (09-15) → **102 (today)** — crossing the ≥100 bar named as a
watch item in three straight logs (09-13, 09-14, 09-15: *"if the doubling pattern holds one
more window this crosses 100"*). Position 66.9, clears the `>50` half of the compound
condition (`≥100 impr AND position >50`) with room. This is the first time in the loop's
history (16 windows tracked, 15 of them at zero) that Rule 5's entry bar is met rather than
watched.

---

## Previous-day comparison / previous action impact

Not byte-identical to 09-15 (window rolled +1 day on both ends) — a fresh firing. 09-15
shipped `557897f` (Rule 6 #3, internal links on `/surf-lesson-prices-bali/`); its own log
registered **no ranking/click claim** for a mechanical, additive linking action, so today's
one-day move (12.9 → 12.7, improving) is noted, not scored. Same non-claim applies to
09-14's target (`/beginner-surf-lessons-bali/`, flat at 33.0 for a second window).
`IMPACT: pending (normal 2–7 day crawl delay)` for both, consistent with 09-15's own framing.

---

## Ladder walk (fresh 155-row page-query join, `dimensions: ['page','query']`, temporary
script written and deleted immediately after use — same pattern as every run since 09-08)

```
ZASADA NADRZĘDNA (content rhythm) — ESCALATED, TICKET STILL UNANSWERED.
  `git log --since=2026-08-21 -- . ':!docs/seo-log'` shows no owner commit since the
  ticket was opened (checked: only loop commits appear). Day 26 of the ticket, 33 days
  since the last loop-added MDX post (2026-08-14, surf-lessons-for-women-bali). Per v4's
  standing instruction for a run that finds the ticket unanswered: descend the ladder,
  say so once. Done.

RULE 1 — NO MATCH (21st consecutive run). Band 10–20, impr≥10: same nine pages as 09-15,
  no entrants, no exits.
  | Page | Verdict |
  |---|---|
  | /batu-bolong-surf/ | Read 8 CONTROL, orthogonality-locked to 09-22 (unchanged) |
  | /blog/canggu-surf-forecast/ | "named and still losing", cited from 09-12, not re-derived |
  | /blog/batu-bolong-beach-canggu-guide/ | Read 8 treatment arm, open to 09-22 |
  | /surf-lesson-prices-bali/ | rejected by v4 popr. 9 (unchanged) |
  | / (homepage) | excluded on the two non-expiring 09-08 grounds (unchanged) |
  | /blog/kids-surf-lessons-canggu/ | query-anonymous — checked in the fresh join: zero rows |
  | /blog/intermediate-surf-coaching-canggu/ | only named query "intermediate surf lessons"
      3 impr @ 18.3 — identical to 09-15's reading, no new lever |
  | /blog/batu-bolong-surf-forecast/ | query-anonymous — checked: zero rows |
  | /blog/private-vs-group-surf-lesson-bali/ | named query "group surf lessons" still
      4 impr @ 49.8 (byte-identical to 09-13/09-15), naming already saturated, "named and
      still losing" verdict stands unchanged |

RULE 1-B — NO CANDIDATE. Seminyak pool re-checked: 50 impr (flat vs 09-15, same total),
  position on its owning page 27.5 → **27.9** (continues the slow monotonic drift, still
  not a fall from inside the band). Both remaining Rule 1-B gates ("was once a strike
  target", "fell from inside the band") stay unmet on the file's own git history — 09-15's
  negative close stands, not re-argued. canggu-vs-uluwatu-beginners' own 1-B strike remains
  closed FAILED (08-28); revert guard (0 clicks AND pos≥40) does not fire at 36.3.

RULE 2 — BLOCKED by the escalation (day 26). Ownership audit deliberately not re-run.

RULE 3 — NO MATCH, verified at page level for a 15th run. All **41** queries at ≥5
  impressions in this window resolve to an owning page in the fresh 155-row join (checked
  by script, zero unowned). Same two standing marginals as prior runs — magicseaweed
  berawa (6 impr @ 9.2, competitor-brand, owned) and batu bolong surf lessons (9 impr @ 9.6,
  doorway risk, already page 1, owned) — upheld without re-argument.

RULE 4 — NO CANDIDATE. Pages ≥20 impr, CTR<2%, re-derived not inherited — same 11 pages as
  09-15, all on the same standing grounds (Read 8 control/arm ×2, rank-bound ×4, tested
  verdict ×1 [canggu-surf-forecast], no-defect-found ×1 [canggu-surfing-safe-for-kids],
  reopen bar unmet ×1 [when-to-book, 44 impr vs >60], null-read-closed ×1
  [family-surf-lessons-bali, Read 2 closed UNDERPOWERED yesterday]).
  `/blog/best-time-to-surf-canggu/` (71 @ 1.4%) stays under the <2% bar for a third day and
  **recovered** on rank (51.0 → 49.8) rather than worsening further — still excluded as
  rank-bound; no change to its status.
  `/blog/private-vs-group-surf-lesson-bali/` (18 @ 18.7, 0.0%) is now 2 impr short of the
  ≥20 Rule 4 entry bar (was 3 short yesterday); not promoted.

RULE 5 — FIRES (see snapshot section above for the trigger). Alt-text branch not
  re-audited a fourth time — checked clean 09-08 and 09-13 on a component tree that has
  not changed since (verified via git log on About.tsx/Gallery.tsx/Hero.tsx: last touch
  predates both checks).

  **File-by-file check of what the image surface actually contains.** `public/images/`
  holds exactly 10 files. The homepage's `IMAGE_GALLERY_SCHEMA` (added 2026-04-30, the
  loop's only prior Rule 5 action on this component) mapped only `GALLERY_PHOTOS` — the
  6 images driving the visible carousel — to `ImageObject` entries. `src/app/sitemap.ts`'s
  per-page `images[]` arrays additionally cover 3 more (the portrait and beginner-lesson
  photos used on `/surf-lesson-prices-bali/` and `/beginner-surf-lessons-bali/`, plus the
  all-ages photo, all folded into the homepage's own sitemap entry). That accounts for
  9 of 10 files. The 10th, `rocky-surf-instructor-about-canggu-bali.jpg`, is rendered by
  `<About/>` on the homepage with `priority` (loaded eagerly, above the fold on desktop)
  and has a correct, descriptive alt attribute — but appears in **zero** structured data
  and **zero** sitemap `images[]` entries anywhere in the repo. Confirmed by grep: the
  filename occurs in exactly one file (`About.tsx`) before this change.

  **Action taken**: added a 7th `ImageObject` to the homepage's `IMAGE_GALLERY_SCHEMA.image`
  array for this photo, via a new `ABOUT_PHOTO` constant concatenated into the mapped list
  — `GALLERY_PHOTOS` itself (and therefore the visible carousel order) is untouched.
  `sitemap.ts` was deliberately **not** touched: Step 5's allowed-scope note restricts that
  file to "adding a new route not auto-detected," which this isn't, so the equivalent
  sitemap gap (this image is also absent from the homepage's `images[]` sitemap array) is
  left unfixed and noted below for the owner/next META-run rather than worked around.

  No ranking/click claim: image search has recorded exactly **0** clicks in every window
  this loop has measured, including today's. The action is coverage completeness for an
  image surface that just crossed the volume bar for the first time, not a predicted
  metric move — same framing 09-15 used for Rule 6's internal-linking action.

=> RULE 5 FIRES.
```

```
ACTION: Rule 5 — Image SEO
TARGET: src/app/page.tsx
RATIONALE: Rules 1–4 all re-verified no-match/no-candidate/blocked on a fresh 155-row
        page-query join. Rule 5's ≥100-image-impression bar (by_search_type: 102 impr,
        pos 66.9) was crossed for the first time after three consecutive doubling windows
        flagged as "watched" in the 09-13/09-14/09-15 logs. A file audit (not a re-check —
        the component tree was read fresh) found one of the site's 10 photographed images,
        rendered above the fold on the homepage, present in zero structured data despite
        8 of the other 9 being covered via the homepage ImageGallery schema or per-page
        sitemap image arrays.
TARGET-ADDITIONAL: none — single-file, additive change (+9/−1 lines net +8, one new
        constant plus one array-spread edit).
HYPOTHESIS: no ranking/click claim. Image search has 0 clicks recorded in every window to
        date; this closes a real structured-data coverage gap on an image surface that has
        just become large enough to matter, not a predicted metric move.
```

---

## Implementation

Files changed: `src/app/page.tsx` (1 file, +9/−1 lines — added an `ABOUT_PHOTO` constant
and changed `IMAGE_GALLERY_SCHEMA.image` from `GALLERY_PHOTOS.map(...)` to
`[...GALLERY_PHOTOS, ABOUT_PHOTO].map(...)`; `GALLERY_PHOTOS` itself and every other export
unchanged).

`npm run build` — **passed, exit code 0**, all 45 routes compiled including `/`. `npm ci`
was run first (fresh checkout, no `node_modules`). Working tree verified clean before
commit (`git status --porcelain` showed only the one intended file after the throwaway
join script was deleted).

Commit: `c142280` — `seo(daily): add missing About photo to homepage ImageGallery schema —
Rule 5 fires for the first time as image impressions cross the 100 bar`

Deployment: `dpl_27Bw2MauTJa3s7hKSQJrpf64UucK`, aliased to `surfingwithrocky.com` —
**READY** (confirmed via Vercel MCP `list_deployments` + `get_deployment`; build finished
in ~22s, well inside the first 90s wait).

Smoke test: **PASS, same routing note as every run since 09-14.** `WebFetch` on
`https://surfingwithrocky.com/` returned `EGRESS_BLOCKED` (apex domain, session-level
network-policy restriction, not a deployment problem). Fell back to
`mcp__Vercel__web_fetch_vercel_url` against the deployment's own preview URL
(`rocky-o126fsv07-aha-software.vercel.app/`), which serves the identical build aliased to
production. The `application/ld+json` block with `"@type":"ImageGallery"` now contains
**7** `ImageObject` entries (was 6); the 7th has
`contentUrl":"https://surfingwithrocky.com/images/rocky-surf-instructor-about-canggu-bali.jpg"`
and `caption":"Rocky, surf instructor in Canggu, Bali"` — both exactly as written in the
commit, confirmed present in the live response.

---

## READS

### Read 8 — the 08-03 post and its control: **first real reading, today (09-16)**

Registration (09-11): 3 readings at 09-16 / 09-19 / 09-22, verdict scored on the mean of
the three. Data point recorded, **not scored** (2 of 3 readings still pending):

| | Control `/batu-bolong-surf/` | Treatment `/blog/batu-bolong-beach-canggu-guide/` |
|---|---|---|
| Clicks | 12 | 6 |
| Impr | 1450 | 950 |
| Share qualifier | 1450 ÷ (1450+950) = **0.604** (bar ≥0.55, clears) | — |

Share qualifier clears for a 6th consecutive log (0.592 → 0.596 → 0.582 → 0.586 → 0.589 →
0.587 → 0.592 → **0.604** across the run of checks). Both pages stay orthogonality-locked
to 09-22 regardless of how the mean resolves.

### Read 9 — spike vs level: day 6 of 7 by exposure, not yet fully visible by GSC's own lag

Registered 09-11, scores clicks over 09-09→09-15 on 09-19 (≥18 LEVEL, ≤12 SPIKE, 13–17
closed indeterminate). GSC's 3-day lag means today's window still only resolves through
09-13: `by_date` gives 09-09 (2 cl), 09-10 (3 cl), 09-11 (0 cl), 09-12 (2 cl), 09-13 (2 cl)
— **running total 9 over the first 5 of 7 days**, with 09-14/09-15 not yet visible. Not
touched further; bars stay as registered.

### The 08-31 canonical sweep — three arms

| Arm | Verdict | Evidence (today) |
|---|---|---|
| Arm 2 — `/blog/best-time-to-surf-canggu` | CONVERGED (unchanged) | Slash-less form absent again from the 41-row page report (7th absence). |
| Arm 1 — `/surf-lessons-canggu` | Not yet converged | No-slash form still present at 2 impr / pos 9.0; slash form 1 impr / pos 5.0. Both forms alive, re-check next run. |
| Arm 3 — `/blog/what-to-expect-first-surf-lesson-bali` | Unmeasurable | Absent from the 41-row page report in *both* forms — the page draws zero impressions this window too. |

### Read 2 — `/blog/family-surf-lessons-bali/` CTR rescue: CLOSED yesterday (09-15), no reopen

Closed UNDERPOWERED per the 08-29/09-12 pre-registration. No replacement read opened; the
page's flat position (8.5 → 8.4) is noted in the top-pages table only.

---

## 🎫 STANDING FAILURE 1 — the content escalation, day 26

Options unchanged: **(a)** keyword research from outside GSC, **(b)** lift the editorial
ban on the Uluwatu pool, **(c)** approve a post without data support. Not invented today.

## 🎫 STANDING FAILURE 2 — Rule 7's two branches share one weekly budget

Not applicable today — the ladder resolved at Rule 5, before reaching Rule 6 or Rule 7.
Still unfixed in the playbook.

## 🎫 STANDING FAILURE 3 — the loop has no mechanism to amend itself

Unchanged. Rule 8 (META-run) requires ≥3 consecutive HOLDs immediately before today; today
is not a HOLD (Rule 5 fired), so Rule 8's entry condition is moot regardless.

## 🆕 New gap named, not fixed — sitemap image coverage

`rocky-surf-instructor-about-canggu-bali.jpg` is now covered by the homepage's structured
data but is still absent from `src/app/sitemap.ts`'s `images[]` array for `/`. That file is
out of today's edit scope (Step 5 restricts `sitemap.ts` edits to new routes) even though
Rule 5's own action list names `<image:image>` sitemap entries as an option. This is a
scope/action mismatch, not a judgement call — worth a line in a future Rule 8 META-run if
it recurs, but one occurrence doesn't meet Rule 8's ≥3-instance bar today.

---

## Notes for tomorrow

1. **Rule 5 fired for the first time in the loop's history.** If image impressions keep
   rising, the next lever (per the rule's own option list) would be the alt-text/filename
   branches — both re-checked clean today — or `sitemap.ts`'s `images[]` arrays, which
   need the owner (see the gap named above) since that file's edit scope is currently
   narrower than Rule 5's own action menu.
2. **Read 8's first real reading is in**: control 12 clicks / 1450 impr, share 0.604
   (6th straight clearance, rising). Two more readings due 09-19 and 09-22 before the mean
   is scored.
3. **Read 9 is at day 6/7 by exposure but only day 5/7 by GSC visibility** (3-day lag) —
   running total 9 clicks over the visible 5 days. Due 09-19; the two invisible days
   (09-14, 09-15) will resolve by then.
4. **`/blog/best-time-to-surf-canggu/` recovered** (51.0 → 49.8) after two windows of
   sharp worsening — still rank-bound and excluded from Rule 4, but the slide named on
   09-15 did not continue.
5. **`/blog/private-vs-group-surf-lesson-bali/` is 2 impressions short of Rule 4's ≥20
   entry bar** (was 3 short yesterday, 5 short the day before) while still sitting in
   Rule 1's band with an unchanged "named and still losing" verdict — watch whether it
   crosses into Rule 4 territory before Rule 1's exclusion would matter again.
6. **Rule 6's weekly budget (reset 09-14) is 2/2 spent** until Monday 09-20 — irrelevant
   today since Rule 5 fired first, but the next HOLD-shaped day will hit it again.
7. **Canonical-sweep Arm 1** (`/surf-lessons-canggu`) still has both URL forms alive
   (2 impr no-slash, 1 impr slash) — re-check next run rather than calling it converged
   or stalled prematurely.
8. **WebFetch confirmed `EGRESS_BLOCKED` on the apex domain again this session** — the
   `mcp__Vercel__web_fetch_vercel_url` fallback on the deployment's own `.vercel.app` URL
   remains the documented, working path (Step 9/10 of `.claude/commands/daily-seo.md`).
9. Page↔query join still absent from `scripts/gsc-snapshot.mjs` — carried a **fifteenth**
   run, outside the loop's edit scope, needs the owner.

---

## Addendum

None — single firing today.
